/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["seq2seq-attention"] = {
  "id": "seq2seq-attention",
  "title": "Seq2Seq and the Birth of Attention",
  "titleNe": "Robin at the Poneglyph: she does not memorise the stone and walk away",
  "intro": "reading one sequence and writing another, the single vector that could not hold a paragraph, and the 2015 patch that turned out to be the whole future",
  "slides": [
    {
      "heading": "Sequence in, a different sequence out",
      "narration": "Everything we have built so far turns a sequence into one thing: a label, a number, a class. Translation does not have that shape. You read a sentence in Nepali and you write a sentence in English, and crucially the output length is not determined by the input length — a five word source can be a nine word target, and you do not know which until you are most of the way through writing it. Summarization has the same shape, so does question answering, so does code generation. Sequence in, different sequence out, lengths unrelated. That needs a new architecture, and the one the field settled on in twenty fourteen is the encoder-decoder, and it is exactly what the name says. Take one recurrent network — the encoder — and feed it the source sentence, one token at a time, with no output at all. You are not asking it to produce anything; you are just letting it read. When it reaches the end, its final hidden state is a vector that summarizes, in some learned way, everything it just read. That vector is called the context vector, and it is handed to a second network — the decoder — as its starting state. The decoder's job is to generate. And generation works differently from anything you have written so far, because it is a loop rather than a single forward pass. You start it with a special start-of-sequence token. It produces a distribution over the vocabulary, you pick a word, and then — this is the important part — you feed that word back in as the next input, along with the updated hidden state. It produces the next word. And so on, until it produces a special end-of-sequence token, at which point you stop. Notice what that gives you for free: the model chooses its own output length, by deciding when to emit the end token. Nothing in the architecture had to be told how long the answer is. Now, training, and there is a genuine problem here worth understanding. At the start of training the decoder is terrible. If you let it feed its own outputs back in, then it produces a wrong first word, and the second word is now conditioned on a wrong first word, and the whole sequence derails, and the gradient signal is mostly noise about a sentence that has nothing to do with the target. Learning is glacial. The standard fix is called teacher forcing: during training, ignore what the model actually predicted and feed in the true previous token from the target sentence instead. Now every step is conditioned on a correct history, the model learns the local mapping quickly, and training converges. It is a good trick and it is what everyone uses. But it introduces a mismatch you should be able to name: during training the model only ever sees perfect history, and at inference it only ever sees its own history, mistakes included. That gap is called exposure bias, and it is why a generated sequence can start well and then wander off — the model is in a state it never encountered while learning. There are fixes, they are partial, and it remains a real property of every autoregressive generator, including the very large ones.",
      "bullets": [
        "Translation, summarization and question answering share a shape no classifier has: the input is a sequence, the output is a sequence, and <b>the two lengths are unrelated</b>. Five words in can be nine words out.",
        "The 2014 answer is the <b>encoder-decoder</b>. One RNN reads the source and stops; its final hidden state is handed to a second RNN, which generates the target one token at a time, feeding each output back in as the next input.",
        "Generation is a loop, not a forward pass. It starts from a <code>&lt;sos&gt;</code> token and ends when the model emits <code>&lt;eos&gt;</code> — so the model decides its own output length, which is exactly what the task needed.",
        "<b>Teacher forcing</b> trains it: feed the true previous token rather than the model's own guess, so one wrong word early does not poison the whole sequence. It trains fast and creates <b>exposure bias</b> — at inference there is no true token to feed."
      ]
    },
    {
      "heading": "The bottleneck: one vector cannot hold a paragraph",
      "narration": "Now the flaw, and I want to state it precisely because the fix follows directly from the statement. In the encoder-decoder as described, everything the decoder will ever know about the source sentence arrives through one vector of fixed size. Five hundred and twelve numbers, say. The encoder reads the whole source, compresses it into those five hundred and twelve numbers, and then the source is gone — the decoder never sees it again. It generates the entire translation from that summary alone. For a short sentence this is fine. Five hundred and twelve numbers is plenty of room for six words. But the size does not grow with the input. A fifty-word paragraph with three clauses, two names and a negation gets exactly the same five hundred and twelve numbers, and it does not fit. Something has to be discarded, and the network has no way to know in advance which parts the decoder is going to need. This is the information bottleneck, and it is not a subtle theoretical worry — it was measured, immediately, and the shape of the result is dramatic. Translation quality holds up well to about twenty tokens and then falls off a cliff. Not a gentle decline as sentences get longer, which is what you would expect from a task simply getting harder. A collapse. That shape is the signature of a hard capacity limit being hit, and it told everybody that the problem was structural rather than a matter of more training or more data. There is a second, related symptom worth naming, because it explains what the failures actually look like. The encoder's final hidden state was, by construction, most recently updated by the last few tokens it read. Everything earlier has been passed through the recurrence dozens of times and partially overwritten. So the context vector is systematically biased toward the end of the source sentence, and in a long input, the beginning is faint. Translations lose their opening clauses. Summaries forget the first paragraph. This is the fading-memory problem from the RNN lesson, showing up in a new place. Now, the obvious question, and it is worth walking through because the answer is instructive: why not just make the vector bigger? Because it does not solve anything. Whatever size you choose is fixed, and there is always a longer input that overflows it — you have moved the cliff, not removed it. And a very large context vector is wasteful and harder to train on short sentences, where most of it is empty. You cannot buy your way out of a structural constraint with more numbers. The fix has to change the structure. And the change, when it came in twenty fifteen, is almost embarrassingly simple to state: stop forcing everything through one vector. Keep all of the encoder's hidden states — one per source token — and let the decoder look back at whichever ones it needs, at each step of generation. That is attention.",
      "bullets": [
        "Everything the decoder knows about the source arrives through <b>one fixed-size vector</b>. A five-word sentence and a fifty-word paragraph get the same 512 numbers, and the second one does not fit.",
        "The symptom is unmistakable and was measured in 2014: translation quality is fine to about 20 tokens and then <b>falls off a cliff</b> as length grows. Not a gentle decline — a collapse.",
        "It is also a <b>recency problem</b>. The encoder's final state was most recently updated by the last few tokens, so the beginning of a long sentence has been overwritten by the end of it.",
        "Enlarging the vector does not fix it: any fixed size is eventually too small, and a bigger one overfits on short inputs. <b>The constraint is structural</b>, which is why the fix had to change the architecture rather than the numbers."
      ]
    },
    {
      "heading": "Attention: look back at everything, weight what matters now",
      "narration": "Here is the mechanism, and it is worth going slowly because you will meet this exact computation in every model for the rest of the course. Change one thing about the encoder: instead of throwing its intermediate states away, keep all of them. One hidden state per source token, so a twenty-word sentence leaves twenty vectors on the table. The source is no longer summarized; it is still there, laid out. Now the decoder. At each step of generation, before it produces the next word, it does three small things. First, scoring: take the decoder's current hidden state — which represents roughly what am I about to write — and compare it against every one of the encoder states, producing one number per source position that says how relevant this source word is to what I am doing right now. Second, softmax: push those scores through a softmax so they become positive and sum to one. You now have a set of weights across the source sentence — a distribution of attention. Third, the weighted sum: multiply each encoder state by its weight and add them all up. The result is a single vector, the same shape as before, and the decoder uses it exactly as it used the old context vector. So what has actually changed? The context vector is no longer computed once. It is recomputed at every single output step, and each time it is a different blend of the source. When the decoder is writing the subject of the sentence, the weights concentrate on the source subject; when it moves to the verb, the weights move too. The decoder gets a fresh, task-specific view of the source for each word it writes. That is the entire idea, and it dissolves both problems at once: nothing has to be compressed into a fixed budget because the whole source is available, and the beginning of a long sentence is exactly as reachable as the end because it is one weighted sum away rather than three hundred recurrence steps away. Two things are worth emphasizing before we move on. The first is that none of this is hand-designed. Nobody writes rules about which source words matter for which target words. The scoring function is a small learned network, trained by the same translation loss as everything else, and the alignment behaviour appears because it is useful for getting the translation right. The second is a gift nobody planned. Because the attention weights are just numbers, you can plot them: source words along one axis, generated words along the other, brightness for weight. And when people did that in twenty fifteen, they saw a word alignment — a visible diagonal, with clean off-diagonal jumps exactly where the two languages reorder. You could see the model looking at la maison while it wrote the house. After years of neural networks being opaque, here was a model that showed its work, and that legibility is a large part of why attention was adopted so fast.",
      "bullets": [
        "Keep every encoder state, not just the last. At each decoding step, <b>score</b> the decoder's current state against all of them, <b>softmax</b> the scores into weights that sum to one, and take the <b>weighted average</b> of the encoder states.",
        "That average is a fresh context vector, recomputed for every output token. The decoder gets a different view of the source for each word it writes — which is the whole difference.",
        "Nothing is hand-designed. The scoring function is learned, so the model works out for itself which source positions matter when writing each target word, from the translation loss alone.",
        "<b>It also made the model legible.</b> Plot the weights as a matrix and you get a soft word alignment — visible proof that <i>la maison</i> lit up when <i>the house</i> was written."
      ]
    },
    {
      "heading": "Robin at the Poneglyph: she does not memorise the stone",
      "narration": "Ohara is the right place to end this, because Robin is a translator and translation is exactly the task, and the two designs we have just compared are two different scholars. Imagine the first one. She stands in front of the Poneglyph and reads the entire inscription carefully, top to bottom. Then she closes her eyes, turns away from the stone, and recites the translation from memory. For a short inscription this is genuinely impressive and it works. For a wall of glyphs it does not, and notice what specifically fails: not the whole thing, but the beginning. By the time she is reciting the ending, the opening lines have been crowded out by everything that came after them. That is the fixed context vector, and that is the recency bias, and if you have ever tried to summarize a long document from memory you know the feeling exactly. Now watch what Robin actually does, because it is completely ordinary and it is the entire mechanism. She keeps the stone in front of her. She writes one line of translation, and while writing it, her eyes go back to the particular glyphs that line came from. Then she writes the next line, and her eyes move to a different part of the stone. She is not holding the inscription in her head at all — she is holding a place in her work, and looking up whatever the current place requires. She might glance at one glyph three separate times, for three different lines, because it matters to all three. And that is attention, with nothing lost in the translation of the metaphor. The stone still standing there is the encoder states, kept rather than discarded. The glance is the scoring step, asking which part of this do I need for what I am writing now. What she is looking at in this moment is the context vector for this word — recomputed, every line, from the same unchanged source. And the fading problem simply does not arise, because the beginning of the inscription is as available at the end of the work as it was at the start. It never had to survive being carried; it never moved. There is one more thing in the picture, and it is the gift I mentioned. Suppose you sat beside Robin and could not read either the glyphs or her handwriting. You could still watch her eyes. You would see that when she wrote this line she looked there, and when she wrote the next she looked somewhere else, and from her eyes alone you could build a map between the stone and the page. You would learn something real about the structure of the translation without understanding a single word of it. That is what an attention map is, and it is why this family of models is the most inspectable thing in deep learning. Everything before it kept its reasoning inside its weights. This one looks at things, and you can see where.",
      "bullets": [
        "The bottleneck version of Robin would read the whole Poneglyph, close her eyes, walk away, and recite the translation from memory. For a short inscription that works. For a wall of glyphs it does not, and the opening lines are what she loses.",
        "What she actually does is keep the stone in front of her. As she writes each line, her eyes go back to the specific glyphs that line came from — a different part of the stone for every line she writes.",
        "<b>That is attention, exactly.</b> The stone is the encoder states, the glance is the scoring, and what she is looking at right now is the context vector for the word she is writing right now.",
        "And an observer could follow her eyes and see the alignment between stone and page without reading either. That is the attention map — the reason this architecture is the most inspectable one in deep learning."
      ]
    },
    {
      "heading": "From attention-as-patch to attention-as-architecture",
      "narration": "Let us finish by placing this properly in the story, because the twenty fifteen paper is not the famous one and it is important to understand why. Attention, as Bahdanau and colleagues introduced it, was a repair. The architecture was still an encoder-decoder made of recurrent networks; attention was bolted onto the side to fix the bottleneck. And it worked — long-sentence translation stopped collapsing, quality jumped, and within a couple of years every serious sequence-to-sequence system had it. But look at what it did not fix. Underneath the attention, the encoder and the decoder were still RNNs, and an RNN still processes one token at a time, each step waiting for the last. So the second complaint from the CNN and RNN lesson — the one about hardware, about a GPU with thousands of cores sitting almost idle — was completely untouched. You had a model that could now remember a long sentence and still could not be trained quickly on a long corpus. So the question that produced the next architecture is, in hindsight, the obvious one, and it is worth sitting with for a moment because this is what good research often looks like. Attention is doing the real work of connecting positions. The recurrence is expensive, it is the thing forcing everything to be sequential, and now that attention exists, what exactly is it still contributing? And the answer, tested in twenty seventeen, was: not enough to keep. Take the recurrence out. Keep only attention. That is the transformer, and the title of the paper — Attention Is All You Need — is a literal description of the experiment. Two things fall out of that removal immediately, and they are the two complaints answered at once. First, with no step-to-step dependency, every position in the sequence can be computed simultaneously. The whole sentence goes through the model in parallel, the graphics card is finally doing what it was built for, and training on enormous corpora becomes practical — which is the door to every large model that followed. Second, the path length between any two positions is now one. Not three hundred, not ten. Position one and position five hundred are a single attention step apart, so distance stops mattering at all. There is a cost, and it is worth naming so the picture is honest. Recurrence was doing one useful thing quietly: it knew what order the words came in, because it read them in order. Remove it and attention, which is just a weighted sum, has no idea whether a word was first or last — shuffle the input and the output is unchanged. So order has to be added back deliberately, as positional encodings, and that is one of the first things the next lesson deals with. But that is a detail to be engineered, not a wall. Everything from here to the end of this course — BERT, GPT, the model you will build in the code lab, and the largest systems running in the world today — is this slide, scaled up.",
      "bullets": [
        "Attention arrived in 2015 as a repair bolted onto an RNN. It fixed the memory problem completely — and left the other one untouched, because the recurrence underneath was still strictly sequential.",
        "So the 2017 question was the obvious one in hindsight: if the lookback is doing the real work, <b>what is the recurrence still for?</b> Remove it and keep only attention.",
        "That is the transformer. Two consequences fall out at once: every position computes in parallel, so the GPU is finally saturated; and any two positions are <b>one step apart</b> regardless of distance.",
        "One thing has to be added back — with no recurrence, nothing knows the word order, hence <b>positional encodings</b>. Everything after this slide, up to the largest models running today, is that idea scaled."
      ]
    }
  ]
};
