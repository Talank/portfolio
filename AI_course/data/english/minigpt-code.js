/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["minigpt-code"] = {
  "id": "minigpt-code",
  "title": "Build a Mini-GPT in PyTorch",
  "titleNe": "The Sabaody reunion: nobody learned anything new that day, and everything changed",
  "intro": "every part is already yours — this is the assembly: one attention module, one block, one model, one generate loop, and an honest look at what it costs",
  "slides": [
    {
      "heading": "What you already own, and what is actually left",
      "narration": "This lesson has no new ideas in it, and I want to say that first, because it changes how you should approach it. You already derived causal self-attention — twice, once on paper and once as a formula. You already know the transformer block: attention, feedforward, residual around each, layer norm before each. You already know the objective, which is predict the next token and never look right. Every conceptual piece of a GPT is in your hands. What is left is assembly, and assembly is not nothing. There is a real gap between understanding a diagram and having a thing that runs, and that gap is made almost entirely of details that no diagram shows you. Which tensor is what shape at which line. Where the causal mask actually lives and how it gets onto the GPU. Whether the output projection shares weights with the input embedding. What happens at the very first generation step when there is only one token. None of those are deep, all of them will stop your code, and working through them is what converts a picture in your head into something you could debug at three in the morning. The finished artifact is small, and its smallness is the point. Three classes. CausalSelfAttention, which does one multi-head attention with a triangular mask. TransformerBlock, which wraps that and a feedforward in pre-norm residuals. And MiniGPT, which holds the embeddings, stacks N blocks, and projects to the vocabulary. Plus a generate method, which is a loop. That is roughly two hundred lines of PyTorch, and it is the same two hundred lines, structurally, that are inside the model you are talking to when you use a commercial LLM. The difference between your file and theirs is the width of the layers, the number of them, and about ten million dollars of electricity. One expectation to set before you run it, because people are disappointed by this and should not be. Train this on a few megabytes of text on a laptop for twenty minutes and what you get back will look like English — it will have plausible word shapes, roughly correct punctuation, sentences that start and stop in the right places — and it will mean absolutely nothing. That is the correct outcome. It tells you the architecture, the loss, the masking and the optimizer are all wired up properly, because a broken model produces noise rather than fluent nonsense. Fluent nonsense is the signature of a working small model, and it is genuinely more informative than a good result would be, because it shows you exactly which part of a language model comes from architecture and which part comes from scale.",
      "bullets": [
        "You have derived causal self-attention, the transformer block with its residuals and norms, and the next-token objective. <b>There is no new machine learning in this lesson.</b> There is assembly.",
        "The finished thing is three classes and about two hundred lines: <code>CausalSelfAttention</code> → <code>TransformerBlock</code> → <code>MiniGPT</code>, plus a <code>generate()</code> loop.",
        "The gap between understanding a diagram and running one is entirely made of details — shapes, buffers, weight tying, where the mask lives. <b>Those details are the lesson.</b>",
        "Train it on a few megabytes of text on a laptop and it will produce something that looks like English and means nothing. That is the correct result, and it is more informative than a good one."
      ]
    },
    {
      "heading": "The three classes, and the details that bite",
      "narration": "Let us walk the three classes and stop at the four places people get stuck. First, CausalSelfAttention. The obvious implementation makes three separate linear layers for Q, K and V. Do not. Make one Linear from d to three-d and split the output into three chunks. The mathematics is identical — you are computing exactly the same three projections — but it is one matrix multiplication instead of three, and this code runs in every layer on every forward pass, so the difference is real. Then reshape to separate the heads, and here is where you will spend your debugging time: the tensor goes from batch by time by three-d, to batch by time by heads by head-dimension, and then you transpose so that heads sit next to batch and the attention maths operates on the last two dimensions. Write the shape in a comment at every line. Everyone does this and nobody admits it. Second, the mask. You need a lower-triangular matrix of ones, and the important thing is how you attach it to the module. Do not set it as a plain attribute — use register-buffer. A buffer is state that belongs to the module but is not a parameter: it moves to the GPU when you call dot-to on the model, it is saved and loaded with the state dict, and crucially the optimizer never sees it, because it is not something to learn. Setting it as a plain tensor attribute means it silently stays on the CPU while your model moves to the GPU, and you get a device mismatch error several layers deep with no obvious cause. Third, TransformerBlock, and there is one line worth writing exactly. Pre-norm means: x equals x plus attention of layernorm-one of x, then x equals x plus feedforward of layernorm-two of x. Notice the shape of that. The normalization happens inside the sublayer call, on the way in, and the residual addition happens outside it. That leaves the residual path — the plus x — completely unobstructed from the first block to the last, which is exactly the clean corridor we talked about, and it is why this version trains stably without a learning-rate warmup. The original post-norm version puts the normalization outside the addition and needs babysitting. Fourth, MiniGPT itself, and one trick worth knowing. You have a token embedding matrix that maps vocabulary size to d, at the bottom. You have an output projection that maps d back to vocabulary size, at the top. Those are maps between the same two spaces in opposite directions, and it turns out you can and should use the same matrix for both — that is weight tying. In a small model where the vocabulary is large relative to everything else, the embedding table can be most of your parameters, so tying it halves the model. And it usually improves quality rather than hurting it, because the two roles genuinely are related: a token's input representation and the direction you need to point to predict it should not be independent facts.",
      "bullets": [
        "<b>One projection, not three.</b> Compute Q, K and V with a single <code>Linear(d, 3d)</code> and split the result — same maths, one matrix multiply instead of three, which matters because this runs in every layer of every forward pass.",
        "Register the causal mask with <code>register_buffer</code>, not as a plain attribute. Buffers move with <code>.to(device)</code> and are saved with the model but are <b>not parameters</b>, so the optimizer never sees them.",
        "The block is pre-norm: <code>x = x + attn(ln1(x))</code>, then <code>x = x + ffn(ln2(x))</code>. Write it in that shape and the residual path stays clean — this is the version that trains without a warmup.",
        "<b>Tie the output projection to the input embedding.</b> Both are <code>vocab × d</code> maps between the same two spaces; sharing them saves a large fraction of the parameters and usually improves the model."
      ]
    },
    {
      "heading": "generate(): the loop, and its hidden cost",
      "narration": "The generate method is where the model stops being a classifier and starts being the thing people actually use, and it is four lines. Given a starting sequence of token ids, run the model forward. You get logits at every position, but you only want the last one, because that is the model's prediction for what comes next — throw the rest away. Convert those logits into a probability distribution and sample a token from it. Append that token to your sequence. Go back to the top. Do that a hundred times and you have a hundred tokens. That is it. Every chat interface you have ever used is this loop with a network connection wrapped around it, and the fact that you can see the text appearing word by word is not a design flourish, it is the loop running. Two details will bite you. The first: crop the context. Your model has a fixed block size, because the position embeddings are a table with a fixed number of rows. If the generated sequence grows past that, you index off the end of the table and crash — and you crash a hundred tokens in, after a generation that looked like it was going well, which is an annoying way to lose a run. So before each forward pass, take only the last block-size tokens. Notice what that means behaviourally: the model has a hard horizon, and text that scrolls off it is simply gone. The second: no-grad and eval. You are not training, so wrap the whole loop in torch dot no-grad, which stops autograd recording the tape. On a model of any size the tape is most of your memory, and here it is pure waste — memory you would much rather spend on a longer context. And call model dot eval so dropout is off and any normalization uses its running statistics. Now, the hidden cost, and it is the reason a later lesson exists. Look at the loop again. At step one you run the model on the full prefix. At step two you append one token and run the model on the full prefix again. At step three, again. Every step recomputes the keys and values for every token you have already processed, and none of those have changed — they cannot change, because the causal mask means a token's representation never depends on anything after it. So the naive loop does an enormous amount of provably redundant work. Generating n tokens costs on the order of n cubed operations, when it should cost far less. This is not a small constant factor. It is the difference between a model responding in a second and a model responding in a minute, and it is why every serving stack has a KV cache — you store the keys and values you have already computed and only compute the new column each step. You will build one later. The point of meeting the problem here, in your own two-hundred-line file, is that when you meet the fix you will recognize exactly what it is fixing.",
      "bullets": [
        "The loop is four lines: forward the sequence, take the logits at the <b>last position only</b>, sample a token from them, append it, repeat. Everything a chat interface does is this, wrapped in a stream.",
        "Crop the context to the model's block size before every forward pass, or the position embeddings run out of table and you get an index error a hundred tokens into a nice-looking generation.",
        "Wrap it in <code>@torch.no_grad()</code> and call <code>model.eval()</code>. You are not learning here, and the tape would cost you memory you would rather spend on a longer context.",
        "<b>The hidden cost:</b> the naive loop re-runs the whole prefix for every new token, so generating n tokens is O(n³) work. Everything already computed is recomputed. The fix — caching the keys and values — is a later lesson, and now you know why it exists."
      ]
    },
    {
      "heading": "Sabaody: nobody learned anything new that day",
      "narration": "Two years before Sabaody, the crew is scattered across the world and every one of them trains alone. Zoro is on Gloom Island learning to cut things he cannot see. Sanji is at Kamabakka learning a leg technique built out of heat. Luffy is on Rusukaina doing the same four moves ten thousand times a day. Nami studies weather she could not previously read. Each of them, separately, becomes good at one specific thing, and none of that training is joint. That is the last three parts of this course. You learned attention in isolation. You learned the block in isolation. You learned the objective, and the optimizer, and the loop, each on its own, and none of it was assembled. Now the reunion, and here is the detail I want you to notice, because it is the entire point of this lesson. Sabaody is not a training arc. Nobody acquires a new technique on the day they meet at the Sunny. There is no lesson, no master, no revelation. They walk up the hill, they get on the ship, and they leave. All that happens is assembly — the parts are put in the same place, wired to each other, and pointed in one direction. And the crew that sails out of Sabaody is not the sum of nine improved individuals; it is a qualitatively different thing that can attempt a sea none of them could have entered alone. That is what you are doing today. Causal self-attention on its own is a weighted average with a triangular mask — mildly interesting. A feedforward network on its own is the most ordinary object in this course. A residual connection is an addition. A sampling loop is four lines. Every one of these parts is unremarkable, and you have understood each of them separately for several lessons now. Put them in the right order, with the right shapes, pointed at one loss, and the thing that comes out generates language. Nothing new was learned. Everything changed. There is one more honest note in the picture, and it keeps the lesson from being triumphalist. The crew that reassembles at Sabaody is still, by New World standards, very small. They have not become Yonko. What they have is the right structure, and what they lack is time and scale, and the next two years of the story is them acquiring those the hard way. Your two-hundred-line file is in exactly that position. It has the correct architecture — genuinely the same one — and it has seen a few megabytes of text on one laptop instead of trillions of tokens on ten thousand accelerators. When you read your model's fluent, meaningless output, you are looking at precisely what architecture gives you and precisely what scale gives you, cleanly separated, in one experiment. Very few things in this field are that legible.",
      "bullets": [
        "The crew scattered for two years and each of them trained alone — Zoro on Gloom Island, Sanji at Kamabakka, Luffy at Rusukaina. Separate skills, learned separately, in the last three parts of this course.",
        "The reunion is not a training arc. Nobody acquires a technique on the day they meet at the Sunny. <b>They are simply assembled</b>, and the crew that sails out is a different order of magnitude from the parts that walked in.",
        "That is this lesson exactly. Attention, the block, the objective and the loop are each unremarkable alone. Wired together in the right order, they are a language model.",
        "And the arriving crew is still tiny compared to what is out there. <b>Same people, same techniques, less time and fewer resources</b> — which is precisely the difference between your file and GPT-4."
      ]
    },
    {
      "heading": "Sizing intuition: how big is this thing?",
      "narration": "Let us finish with arithmetic, because being able to estimate a model's size in your head is genuinely useful and almost nobody teaches it. Work per block. Attention has four projection matrices — query, key, value, and the output projection after the heads are concatenated — and each is d by d. So attention is four d squared. The feedforward expands from d to four d and comes back, so that is four d squared going up and four d squared coming down: eight d squared. Add them and one block is about twelve d squared parameters, ignoring the biases and layer norms, which are negligible. Multiply by the number of blocks and the transformer stack is twelve N d squared. Then add the embedding table, which is vocabulary size times d. Notice how those two terms behave differently as you scale, because it explains several design decisions you will see. In a small model, d is a few hundred and the vocabulary is fifty thousand, so the embedding table can easily be more than half of all your parameters — which is exactly why weight tying is such a big win at small scale. In a large model, d is twelve thousand and there are ninety-six layers, so the stack is enormous and the embedding table is a rounding error. Same formula, completely different balance, and it is why advice that is correct for a small model is often wrong for a large one. Put numbers through it. A model with d equal to three eighty-four and six layers gives you twelve times six times three eighty-four squared, which is about ten million parameters in the stack — small enough to train on a laptop CPU overnight or a modest GPU in minutes. GPT-2 small is d equal to seven sixty-eight with twelve layers: twelve times twelve times seven sixty-eight squared is about eighty-five million in the stack, plus a large embedding table, giving the one hundred and twenty-four million everyone quotes. The formula holds all the way up, and you can use it on any model card you read. One final number, and it is the one that answers most practical questions. Training memory is not just the parameters. You need the weights, a gradient for every weight, and — if you are using Adam, which you are — two moment estimates for every weight. That is four copies. In thirty-two-bit floats, a one-billion-parameter model needs four gigabytes for the weights and sixteen gigabytes before you have stored a single activation. This one ratio, four times the parameters, explains almost every why does this not fit on my GPU question, and it is why the memory-saving techniques in the next few lessons — mixed precision, gradient checkpointing, and the low-rank adapters that train a tiny fraction of the weights — exist at all.",
      "bullets": [
        "Per block, attention holds <code>4d²</code> (Q, K, V and the output projection) and the feedforward holds <code>8d²</code> (up to 4d and back). So a block is about <b>12d²</b>, and the whole stack is <code>12·N·d²</code>.",
        "The embedding table is <code>vocab × d</code> — dominant in a small model, negligible in a large one. This is exactly why weight tying matters at 10M parameters and barely registers at 100B.",
        "Sanity-check it: <code>d</code>=384, <code>N</code>=6 is about 10M parameters, trainable on a laptop. GPT-2 small is <code>d</code>=768, <code>N</code>=12, 124M. <b>Same formula, all the way up.</b>",
        "Memory during training is roughly <b>4× the parameters</b> for weights, gradients and Adam's two moments — before activations. That single ratio explains most \"why does this not fit\" questions."
      ]
    }
  ]
};
