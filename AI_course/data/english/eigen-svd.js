/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["eigen-svd"] = {
  "id": "eigen-svd",
  "title": "Eigenvectors, Eigenvalues & SVD",
  "titleNe": "The whirlpool's axis — the one course that doesn't turn",
  "intro": "every transformation has special directions it can't rotate — only stretch; they power PCA, explain gradient explosions, and give LoRA its low-rank idea",
  "slides": [
    {
      "heading": "The directions a matrix cannot turn",
      "narration": "Entering the Grand Line, the Going Merry is caught in a monstrous whirlpool, and every heading the crew tries gets bent — sail north, the current drags you northeast; sail west, you are swept southwest. The whirlpool is a transformation: it takes your intended course, a vector, and outputs a twisted one. Panic on deck. Then Nami studies the spiral and finds it: one line through the maelstrom, the surge axis, where the current does not bend your course at all. Sail exactly along it and the whirlpool only accelerates you — you come out pointing the same way, three times faster. Course unchanged, magnitude times three. Nami found the eigenvector, and the times-three is its eigenvalue. That is the whole definition: apply a matrix to lots of vectors and most come out pointing somewhere new, but almost every matrix has a few special directions where the output is just the input scaled, same line, no rotation — A times v equals lambda times v, where v, not zero, is an eigenvector, and the stretch factor lambda is its eigenvalue. Verify one by hand, always do this once. Let A be bracket bracket two one, bracket one two, and v be bracket one one; then A-v is two plus one, one plus two, which is bracket three three, exactly three times v, so bracket one one is an eigenvector with eigenvalue three. Try v equals bracket one minus one: A-v is two minus one, one minus two, which is bracket one minus one, exactly one times v — a second eigenvector with eigenvalue one. This matrix stretches the one-one diagonal by three and leaves the one-minus-one diagonal completely alone, and now you know everything it does, because any input vector is a mix of those two directions. That last clause is the whole point: eigenvectors are the transformation's natural axes. Written in those axes, a scary matrix becomes just a list of per-axis stretch factors, a diagonal matrix, and diagonalization is simply changing to the coordinate system where the machine is simple. Nami did not fight the whirlpool; she found the axis where it becomes plain multiplication.",
      "bullets": [
        "Apply a matrix and most vectors come out pointing somewhere new — but almost every matrix has special directions where the output is just the input <b>scaled</b>: <code>Av = λv</code>. v is an <b>eigenvector</b>; the stretch factor λ is its <b>eigenvalue</b>.",
        "Verify by hand: <code>A=[[2,1],[1,2]]</code>, <code>v=[1,1]</code> → <code>Av=[3,3]=3v</code> (λ=3). And <code>v=[1,−1]</code> → <code>Av=[1,−1]=1·v</code> (λ=1). A stretches the [1,1] diagonal 3×, leaves [1,−1] alone.",
        "Now you know <b>everything</b> A does, because any input is a mix of those two directions. Eigenvectors are the transformation's <b>natural axes</b>.",
        "Written in those axes, a scary matrix is just a list of per-axis stretch factors (a diagonal matrix). <b>Diagonalization</b> = changing to the coordinate system where the machine is simple."
      ]
    },
    {
      "heading": "Repeated multiplication amplifies the biggest eigenvalue",
      "narration": "Here is the single most consequential fact about eigenvalues: multiply by A over and over, and each eigen-direction gets scaled by its lambda each time, so after k steps it is scaled by lambda-to-the-k. The direction with the largest absolute lambda grows fastest and eventually dominates everything, which means any starting vector, transformed repeatedly, swings toward the dominant eigenvector. At the Knock Up Stream the Going Merry rides exactly this: along the dominant axis, every second multiplies the ship's speed again — lambda, lambda-squared, lambda-cubed — until the ship is launched into the sky toward Skypiea. Glorious when you want to reach the sky island, catastrophic when it is your gradients doing the multiplying through fifty layers. This one fact explains three things you will meet again. First, power iteration, which is your lab: to find the dominant eigenvector, just multiply a random vector by A repeatedly and re-normalize each step, and it converges to the top axis. That algorithm, plus a sparsity trick, is how Google's original PageRank ranked the entire web — the ranking literally is the dominant eigenvector of the web's link matrix. Second, exploding and vanishing gradients, which is Part Three: backpropagation multiplies the gradient by roughly the same weight matrices layer after layer, so a largest eigenvalue-ish factor above one makes gradients blow up exponentially, and below one makes them die exponentially. Deep recurrent nets failed on long sequences for exactly this reason, and the fix — attention and transformers — is Part Five's whole origin story. Third, the stability of any iterative system: if the largest absolute eigenvalue is below one, the loop settles; above one, it diverges. Same math, three faces — a search algorithm, a training pathology, and a stability criterion, all just lambda-to-the-k deciding who wins.",
      "bullets": [
        "Multiply by A over and over: each eigen-direction gets scaled by λ each time, so after k steps, λᵏ. The direction with the largest |λ| grows fastest and dominates — any starting vector swings toward the <b>dominant eigenvector</b>.",
        "<b>Power iteration</b> (your lab): to find the dominant eigenvector, multiply a random vector by A repeatedly, re-normalizing. This IS how Google's <b>PageRank</b> ranked the web — the ranking is the dominant eigenvector of the link matrix.",
        "<b>Exploding/vanishing gradients</b> (Part 3): backprop multiplies by roughly the same matrices layer after layer. Largest factor > 1 ⇒ gradients blow up exponentially; < 1 ⇒ they die. Deep RNNs failed at long sequences for exactly this — the fix (attention) is Part 5's origin.",
        "<b>Stability</b> anywhere: |λ|max < 1 means the loop settles; > 1 means it diverges."
      ]
    },
    {
      "heading": "PCA: eigenvectors of the covariance matrix",
      "narration": "The whirlpool's axis was one matrix's natural direction; PCA finds the natural axes of a data cloud, and it is the same eigenvector idea pointed at data instead of a transformation. Take a cloud of data points, rows are samples, and form its covariance matrix C, where entry i-j is the average of feature-i-minus-its-mean times feature-j-minus-its-mean. That matrix summarizes how the features co-vary, and here is the beautiful part: C's eigenvectors point along the cloud's natural axes, the directions of most spread, and each eigenvalue is the variance along its direction. So principal component analysis is simply: pick the top-k eigenvectors, project your data onto them, and throw the rest away. You keep the k directions carrying the most variance, which is usually the most signal, and you compress — seven-hundred-eighty-four pixel dimensions down to fifty while keeping ninety-five percent of the variance. And this is not a heuristic that happens to work; it is an exact optimization solved exactly. The variance of your data projected onto a unit direction v is v-transpose-C-v, so find the direction of maximum variance means maximize v-transpose-C-v subject to v having length one, and the maximizer of that constrained problem is precisely C's top eigenvector, with the achieved variance being its eigenvalue — that is the Rayleigh-quotient argument, and each next component repeats it in the subspace orthogonal to the ones already chosen. The uses you will actually meet: visualizing seven-hundred-sixty-eight-dimensional embeddings down in two dimensions, decorrelating features for classical models, whitening, and building the intuition that real data lives on a much lower-dimensional manifold than its raw dimension suggests. One warning that is the number-one PCA bug: always center the data first, subtract the feature means, because without centering your top component mostly points at the data's mean rather than its spread. Center, covary, take the top eigenvectors — Nami mapping the surge axes of a whole fleet of data points.",
      "bullets": [
        "Take a cloud of data points. Its <b>covariance matrix</b> C summarizes how features co-vary: <code>Cᵢⱼ = avg of (featureᵢ−meanᵢ)(featureⱼ−meanⱼ)</code>. C's eigenvectors point along the cloud's natural axes — the directions of most spread — and each eigenvalue is the variance along its direction.",
        "<b>PCA</b> = pick the top-k eigenvectors, project your data onto them, throw the rest away. Compress 784 pixel-dimensions to 50 while keeping 95% of the variance.",
        "It's not a heuristic — it's an exact optimization: the max-variance direction subject to ‖v‖=1 is exactly C's top eigenvector (the Rayleigh-quotient argument). PCA is that optimization, solved.",
        "Real uses: visualizing 768-D embeddings in 2-D, decorrelating features, whitening, and the intuition that \"data really lives on a low-dimensional manifold.\" <b>Always center first</b> — the #1 PCA bug."
      ]
    },
    {
      "heading": "SVD: every matrix is rotate → scale → rotate",
      "narration": "Eigendecomposition needs a square, nicely-behaved matrix, but the singular value decomposition works for any matrix at all, even a rectangular one, and it is the tool you will reach for most. It says A equals U-Sigma-V-transpose, where U and V are rotations, matrices with orthonormal columns, and Sigma is a diagonal of non-negative singular values, sigma-one greater than or equal to sigma-two and so on. Read it as a sentence: every linear machine, however messy, is secretly rotate the input axes, then stretch along each axis by sigma-i, then rotate to the output axes. The singular values tell you how much each independent channel of the machine actually matters. And here is the payoff, the reason SVD is everywhere: low-rank approximation. Keep only the top-k singular values and their vectors and you get the best possible rank-k approximation of A — that is the Eckart-Young theorem, best is a proved guarantee, not a hope. Real-data matrices almost always have singular values that decay fast, so a thousand-by-thousand matrix is approximately a rank-twenty product of two thin matrices: forty thousand numbers standing in for a million. That single deal is image compression, it is latent semantic analysis, which is SVD on the TF-IDF matrix and was early embeddings before the word existed, it is recommender systems, the Netflix Prize was won on it, and it is the philosophical core of LoRA in Part Six. LoRA assumes the fine-tuning update, delta-W, is low-rank, and instead of learning the whole update it learns just two thin matrices whose product is the update — and Eckart-Young is exactly why that bet is sensible: if the best low-rank approximation of the true update captures most of it, you lose almost nothing by never leaving the low-rank family. There is also a clean bridge worth knowing for interviews: the singular values of A are the square roots of the eigenvalues of A-transpose-A, which is always square, symmetric, and non-negative, so SVD is eigendecomposition made safe for any matrix. That is why np-dot-linalg-dot-svd is often the most numerically trustworthy tool in the drawer, and why PCA is usually implemented as the SVD of the centered data rather than the eigendecomposition of the covariance.",
      "bullets": [
        "Eigendecomposition needs square, nice matrices. The <b>Singular Value Decomposition</b> works for ANY matrix, even rectangular: <code>A = UΣVᵀ</code> — U, V are rotations; Σ is a diagonal of non-negative <b>singular values</b> σ₁≥σ₂≥…",
        "Read it as: every linear machine, however messy, is secretly <b>rotate input axes → stretch each axis by σᵢ → rotate to output axes</b>. The σᵢ say how much each independent \"channel\" matters.",
        "<b>Low-rank approximation</b> (the payoff): keep only the top-k singular values and you get the <i>best possible</i> rank-k approximation (Eckart–Young). If σ decays fast (it usually does), a 1000×1000 matrix ≈ a rank-20 product of two thin matrices: 40,000 numbers instead of 1,000,000.",
        "This is image compression, latent semantic analysis (early embeddings!), recommender systems (Netflix Prize), and the core of <b>LoRA</b>: assume the fine-tuning update ΔW is low-rank, learn just two thin matrices."
      ]
    },
    {
      "heading": "Transformed, yet unmoved (what carries forward)",
      "narration": "Step back and the whirlpool gave you all three core facts in one scene: special directions exist, the surge axis; the eigenvalue is the stretch, times three; and repetition amplifies the dominant one, the Knock Up Stream launching the ship into the sky exactly the way exploding gradients launch a loss to infinity. And Sheldon's spot pins the definition itself — guests come, furniture drifts, game night reshuffles the whole apartment, but one point is fixed by every rearrangement, transformed yet unmoved, eigenvalue one. That is Sheldon: that is my spot. Carry forward that one fact wears three faces, all the same lambda-to-the-k math. Power iteration and PageRank find the dominant axis by repeated multiplication. Exploding and vanishing gradients are lambda-to-the-k running through deep layers, the reason initialization schemes, gradient clipping, residual connections, and ultimately attention all exist. And PCA is the top eigenvectors of the covariance, the maximal-variance directions. SVD then generalizes the whole idea to any matrix and hands you low-rank approximation, the compression deal behind LoRA, latent semantic analysis, recommender systems, and image compression alike. Three pitfalls to retire now. First, NumPy returns eigenvectors as columns, not rows — vecs-bracket-colon-comma-i pairs with vals-bracket-i — a classic silent bug. Second, always center your data before PCA, or the top component points at the mean instead of the spread. And third, only symmetric matrices, like every covariance matrix, are guaranteed real eigenvalues; a rotation matrix has complex eigenvalues because it turns every direction in the plane, with no real invariant axis at all — which is geometrically obvious the moment you think in transformations rather than symbols. Eigenvectors are natural axes, eigenvalues are stretch factors, repetition compounds the biggest one, and SVD extends all of it to any matrix — that is the spectral toolkit powering PCA, gradient stability, and LoRA for the rest of the course.",
      "bullets": [
        "The whirlpool gives all three core facts in one scene: special directions exist (the surge axis), the eigenvalue is the stretch (×3), and repetition amplifies the dominant one (Knock Up Stream = exploding gradients). Sheldon's spot pins the definition: transformed, yet unmoved.",
        "One fact, three faces: <b>power iteration / PageRank</b> (find the dominant axis), <b>exploding/vanishing gradients</b> (λᵏ through deep layers), <b>PCA</b> (top eigenvectors of covariance). All the same λᵏ math.",
        "<b>SVD generalizes it to any matrix</b> and hands you low-rank approximation — the compression deal behind LoRA, LSA, recommenders, and image compression.",
        "Pitfalls to retire: NumPy returns eigenvectors as <b>columns</b> (<code>vecs[:,i]</code> pairs with <code>vals[i]</code>); <b>center before PCA</b> or the top component points at the mean; only <b>symmetric</b> matrices are guaranteed real eigenvalues (a rotation has complex ones — it turns every direction)."
      ]
    }
  ]
};
