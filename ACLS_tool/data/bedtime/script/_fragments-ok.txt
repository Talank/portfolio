# Verbless sentences that are meant to have none.
#
# Almost empty on purpose. The shared linter's fragment rule is a Nepali rule —
# a verbless Nepali clause read with silence on both sides lands as a sentence
# that got cut off — and this voyage is mostly English, where a clipped phrase
# is an ordinary beat and reads as deliberate. So the English half is held to a
# density budget instead of a per-sentence one (see fragment_density in
# check_bedtime.py), and only the Nepali lines still need naming here.
