/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["capstone-denden-studio"] = {
  "id": "capstone-denden-studio",
  "title": "Capstone: Assemble DenDen Studio From Every Piece You Built",
  "titleNe": "The Sunny's maiden voyage: every system, built separately, sailing as one",
  "intro": "twenty-one lessons built twenty-one pieces — none ever separate. This doesn't add a mechanism; it draws the map",
  "slides": [
    {
      "heading": "One request, traced end to end",
      "narration": "A creator uploads a photo and a voice sample, writes make her wave and welcome viewers, warm and excited, and clicks go. Follow that one request through everything this course built. Upload: the file streams in chunked and resumable, its bytes verified against a magic-number allowlist, its content hash computed at the door — from backend-api-design — and that hash becomes the cache key every downstream stage will check against. Intake: a VLM reads the photo under the semantics-versus-geometry jurisdiction rule, a deterministic face detector measures the geometry neither should guess at, and the combined verdict decides the pipeline path — from multimodal-models. Voice: the sample is validated for quality and duration, and before anything else touches it, the consent registry checks this voice_id is enrolled, unrevoked, and in scope for this use — from voice-cloning-from-a-sample and safety-consent-ethics. Direction: the creator's paragraph, plus a grounding block of real project facts pulled through read-only tools, becomes one schema'd plan document — stages, voice, gesture timeline, ambient prompt, parameters — from llm-as-director. The plan hits the unified validator; policy violations like an out-of-scope voice reject outright, encoding mistakes like an out-of-bounds keyframe repair in a bounded loop. The creator sees a plain-language preview with a cache-aware cost estimate and approves. Execution: the approved plan, verbatim, never re-generated, goes to the job queue; each stage computes a content key from its actual inputs, checks the cache, and only runs what changed — from media-pipeline-orchestration and hardware-quantization-caching. TTS, coefficients or gesture instantiation, frame rendering, and mux run in dependency order, each hashed and cached the moment it completes. Delivery: progress streams to the browser over SSE with real stage names and percentages, the frontend renders one plan object across every panel — from creator-frontend — and the finished video is tagged with a signed provenance manifest and an in-media watermark before it ever reaches export, from safety-consent-ethics. Evaluation runs in the background the whole time: sync offset, audio and frame quality proxies on this exact render, feeding the golden-set trend line the team watches across every model version, from evaluating-generated-media. One request, every lesson, one flow.",
      "bullets": [
        "A creator uploads a photo + voice sample, writes \"make her wave and welcome viewers, warm and excited,\" clicks go. <b>Upload</b>: chunked, resumable, magic-byte verified, hashed at the door (<i>backend-api-design</i>) — that hash is every downstream stage's cache key.",
        "<b>Intake</b>: a VLM reads semantics, a deterministic detector measures geometry, jurisdiction rule keeps them apart (<i>multimodal</i>). <b>Consent</b>: before anything touches the voice, the registry checks enrolled + unrevoked + in-scope (<i>voice-cloning, safety-consent</i>).",
        "<b>Direction</b>: paragraph + grounded facts → one schema'd plan; validator <b>repairs</b> encoding slips, <b>rejects</b> policy violations; creator approves a cache-aware cost preview (<i>llm-as-director</i>).",
        "<b>Execution</b>: approved plan runs <b>verbatim</b> on the queue, each stage keyed + cache-checked (<i>pipeline, hardware</i>). <b>Delivery</b>: SSE progress, one plan object across panels (<i>creator-frontend</i>), signed+watermarked before export (<i>safety-consent</i>); proxies score this render into the golden-set trend (<i>evaluation</i>)."
      ]
    },
    {
      "heading": "The system, as one map",
      "narration": "Now step back from the single request and look at the whole map: seven components, each with a narrow contract. The frontend holds one plan object and renders every view from slices of it; it talks to nothing but the API. The backend API accepts uploads and job submissions, returns 202 instantly, streams progress, and enforces idempotency — it holds no domain logic of its own, only contracts. The director, an LLM, grounded and constrained, is the only component that turns free text into a plan; it never touches a file, a model weight, or a queue directly. The executor, deterministic workers on the job queue, is the only component that runs models and writes artifacts; it takes plans, never prose. The model pool — Whisper, a TTS engine, a talking-head model, a VLM, sized against the hardware lesson's residency plan — does the actual generation, swappable behind each stage's contract without the executor caring which checkpoint is loaded. The consent and provenance layer gates every synthesis call and tags every output, sitting between the executor and the model pool where nothing can route around it. And the evaluation harness watches everything downstream of the executor, on every render automatically and on a human-reviewed sample, feeding findings back into both the proxy-metric set and the golden set. Draw the arrows and a rule falls out: prose flows in one direction, creator to director to plan, and executable instructions flow in exactly one direction from there, plan to executor to model pool. Nothing downstream of the plan ever produces a new plan or a new prose interpretation — a failure replans by going back to the director with error context, never by improvising at the executor. That single-direction flow is what makes every other invariant in this course enforceable, because you cannot police a boundary that data crosses back and forth across freely. That is the whole architecture: not a pile of models, but a set of narrow contracts with a one-way current running through them.",
      "bullets": [
        "Seven components, each a narrow contract. <b>Frontend</b>: holds one plan object, renders every view from slices, talks only to the API. <b>API</b>: uploads + job submits, 202 instantly, SSE, idempotency — no domain logic, only contracts.",
        "<b>Director</b> (grounded, constrained LLM): the ONLY thing that turns free text into a plan — never touches a file, weight, or queue. <b>Executor</b> (deterministic workers): the ONLY thing that runs models + writes artifacts — takes plans, never prose.",
        "<b>Model pool</b> (Whisper, TTS, talking-head, VLM, sized to the residency plan): swappable behind each stage's contract. <b>Consent/provenance</b>: gates every synthesis call, tags every output, sits where nothing routes around it. <b>Evaluation</b>: watches everything downstream, every render + a sampled review.",
        "Draw the arrows and a rule falls out: <b>prose flows one way</b> (creator → director → plan), <b>instructions flow one way</b> (plan → executor → models). A failure <b>replans at the director</b>, never improvises at the executor — the single-direction flow is what makes every other invariant enforceable."
      ]
    },
    {
      "heading": "The repeated invariants: not duplication, the same defense at every scale",
      "narration": "Five patterns appear, restated, in nearly every lesson from Part One onward, and that repetition was deliberate, not filler. Jurisdiction — semantic judgment to probabilistic components, geometric or deterministic facts to deterministic ones — appears in multimodal intake, in Whisper-plus-voice-activity-detection, in the director-versus-executor split itself, because the same mistake, letting a model guess at something a formula should compute or the reverse, is available at every layer, so the guardrail has to be re-asserted at every layer. Content-addressed caching — hash the real inputs, key the output, skip identical work — appears in stage execution, in the director's cost estimates, in the API's idempotency table, in the golden-set regression diff: one mechanism, reused because have I already done this exact work is a question every layer independently needs answered. Propose, never execute — the LLM emits documents, deterministic code runs them — is the director's entire boundary and also the frontend's relationship to the plan and the API's relationship to a client request; probabilistic judgment stays upstream of anything with real cost or consequence, everywhere it appears. Reject versus repair — fix encoding mistakes, refuse policy violations, and check policy first — shows up in the director's validator and in the consent gate identically, because both need the same asymmetry for the same reason. And scoped, revocable authorization — a grant checked fresh, never assumed — shows up in consent and in idempotency keys and in review triggers; every gate in this system is a small, fast, always-fresh lookup, never a cached assumption. Seeing these five patterns once, in one lesson, would have looked like a clever trick. Seeing them five times, in five different lessons, each time solving a different concrete problem, is what shows they are actual architecture — the load-bearing walls, not decoration repeated for effect. If a new engineer asks whether this is duplicated logic to refactor away, the answer is that the hash function belongs in one utility, but the decision to check freshness before trusting a fact is not duplication — it is the same architectural choice independently defending each layer against its own version of the same risk.",
      "bullets": [
        "Five patterns recur in nearly every lesson — on purpose. <b>Jurisdiction</b> (semantics→probabilistic, geometry→deterministic): in intake, in Whisper+VAD, in the director-vs-executor split — because the same mistake is available at every layer, so the guardrail is re-asserted at every layer.",
        "<b>Content-addressed caching</b> (hash real inputs, key the output, skip identical work): in stage execution, cost estimates, the idempotency table, the golden-set diff — \"have I already done this exact work\" is a question every layer independently needs answered.",
        "<b>Propose, never execute</b> (LLM emits documents, deterministic code runs them): the director's boundary, the frontend's relation to the plan, the API's relation to a request. <b>Reject vs repair</b> (fix encoding, refuse policy, check policy FIRST): the validator and the consent gate, identically.",
        "<b>Scoped, revocable authorization</b> (checked fresh, never assumed): consent AND idempotency keys AND review triggers — every gate is a small, fast, always-fresh lookup. Seen once it's a trick; seen five times solving five different problems, it's <b>load-bearing architecture</b>."
      ]
    },
    {
      "heading": "Building it for real: the minimum slice, and the order that unblocks the most",
      "narration": "Now, for anyone actually building this: a capstone build does not need every model integration to prove the architecture — it needs the full loop, narrow. The minimum viable slice is one path through intake, a single reference-photo category, skip the rest; one TTS engine, skip voice cloning's extra validation; one talking-head model, skip the gesture-library and diffusion families; the complete director to validate to preview to execute to cache chain with real content-hashing, not stubbed, because the caching behavior is exactly what needs proving; one consent check, even a hardcoded single-voice registry; SSE progress end to end; and one proxy metric, sync offset, running automatically. That slice touches every architectural boundary in this course while implementing only a fraction of the model surface, and it is the version worth building first, because it proves or disproves the plan-document-as-lingua-franca bet before a single hour goes into a second talking-head model or a third voice engine. Build order follows what unblocks the most, not what is most interesting or easiest. Content-addressed caching and the job queue come first, because nearly every other component — the director's cost estimates, the API's status reads, the evaluation harness's regression diffs — assumes hashed, cached stages exist beneath it, so building it last means retrofitting that assumption into everything built before it. The plan schema comes next, before any UI, because the frontend, the director, and the executor all need to agree on its shape before any of the three can be usefully built in parallel. The director and a bare-bones executor come before the polished frontend, because a working backend reachable by curl proves the loop; a beautiful UI over a broken loop proves nothing. Consent and provenance are not an add-on-later phase — that is the safety lesson's pitfall restated at project scale — the minimal versions belong in the first working slice, because retrofitting a gate into a system already shipping ungated media is a strictly harder problem than building the gate in from the start. And if time runs short, cut model variety, one voice, one talking-head family, long before cutting any of the five invariants: a narrow system with every guardrail intact is a real foundation; a broad system missing a guardrail is a liability wearing a feature list.",
      "bullets": [
        "A capstone doesn't need every model — it needs the <b>full loop, narrow</b>. Minimum slice: one intake path, one TTS engine, one talking-head model, the complete director→validate→preview→execute→cache chain with <b>real</b> content-hashing, one consent check (even a hardcoded registry), SSE end to end, one proxy metric (sync offset).",
        "That touches every architectural <b>boundary</b> while implementing a fraction of the model surface — worth building first because it proves the plan-as-lingua-franca bet before any second model.",
        "Build order = <b>what unblocks the most</b>. Caching + the job queue FIRST (everything assumes hashed cached stages beneath it). Plan schema next (frontend, director, executor must agree before parallel work). Director + bare executor before the polished UI — a curl-reachable loop proves it; a pretty UI over a broken loop proves nothing.",
        "Consent + provenance are <b>NOT phase-two</b> — the minimal versions ship in the first slice; retrofitting a gate into a system already shipping ungated media is strictly harder. If time runs short, cut model <b>variety</b> long before any invariant: a narrow system with every guardrail is a foundation; a broad one missing a guardrail is a liability with a feature list."
      ]
    },
    {
      "heading": "It doesn't finish — it starts the loop",
      "narration": "The day the Thousand Sunny finally leaves Water Seven is the day every system this crew spent an arc building has to work together for the first time, not separately — the hull rides true because Galley-La's stitched marks are the real ones, checked, not re-litigated mid-voyage; the stores hold because the provisioning tickets were trusted, not re-counted crate by crate; and when a squall hits, nobody improvises a new plan from scratch, they run the course correction through the same roles and checks that built the ship. That is the capstone at full scale. Two last practical things before the loop begins. First, size the box before writing the executor: a live-mode session needs the co-resident VRAM sum of whatever runs simultaneously, held for the session; a batch render needs only the peak of any single stage, since stages run sequentially and release VRAM between them — the hardware lesson's max-not-sum rule. Storage follows the caching manifest directly: renders per week times artifact size times retention window, with eviction-by-cost-to-remake as the release valve when the estimate is wrong, which it usually is, in the direction of creators rendering more than the spreadsheet assumed. The one number worth computing before anything else is cold-cache time-to-first-render, end to end, because if that is not acceptable on the target hardware, no amount of caching or UI polish fixes it later — caching only ever helps the second render of anything, never the first. Second, every lesson's evaluation habit becomes a standing job once this ships: the golden set refreshed against real anonymized usage on a schedule, not once at launch; model upgrades run through the full regression harness with the old model shadowed during a canary period; consent grants expiring and re-confirmed on the registry's own schedule; the audit log's retention enforced on a timer, not by cleanup someone forgets. None of that is new work invented for maintenance — it is the same golden-set, cache-key, and scoped-grant machinery built for day-one correctness, now running continuously instead of once. Watching the coastline disappear, Usopp says what the whole crew is thinking: we built each piece separately and argued about every detail, and somehow, today, it is just one ship. That is the honest way to think about finishing a capstone build: it does not finish. It starts the loop that keeps every invariant this course built from quietly rotting the moment nobody is watching it anymore. Twenty-two lessons, one system, and now it is yours to build.",
      "bullets": [
        "Before the executor's first line, size the box with the hardware numbers: <b>live</b> = co-resident VRAM sum for the session; <b>batch</b> = peak of any single stage (max, not sum). Storage = renders/week × artifact size × retention, eviction-by-cost-to-remake as the release valve.",
        "The one number to compute first: <b>cold-cache time-to-first-render</b>, end to end. If that's unacceptable on target hardware, nothing fixes it later — caching only ever helps the SECOND render, never the first.",
        "After ship, every evaluation habit becomes a <b>standing job</b>: golden set refreshed from real usage on a schedule, model upgrades through the regression harness + shadow canary, consent grants expiring + re-confirmed, audit retention enforced on a timer. Not new work — the same machinery, running continuously instead of once.",
        "Both crews' big day wasn't one system built at the end — it was twenty-one separately-motivated pieces, unretired, discovered to be one architecture all along. A capstone build doesn't finish; it <b>starts the loop</b> that keeps every invariant from rotting the moment nobody's watching."
      ]
    }
  ]
};
