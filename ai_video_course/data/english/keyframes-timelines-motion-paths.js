/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["keyframes-timelines-motion-paths"] = {
  "id": "keyframes-timelines-motion-paths",
  "title": "Keyframes, Timelines & Motion Paths: The Data Model Behind Every Edit",
  "titleNe": "Nami's chart: waypoints, overlay sheets, a knotted string",
  "intro": "the data model that makes generated motion fixable — get it right and the next two lessons are just UI",
  "slides": [
    {
      "heading": "Keyframes: motion a human can hold",
      "narration": "Nami teaches navigation as three stacked disciplines, and the first is the oldest idea in animation. A route, she says, is drawn as a handful of marked waypoints — round this cape, sight this lighthouse, make this bearing by dusk — and the helmsman fills in the smooth sailing between them. That is exactly what a keyframe is: a value pinned at a time, like head pitch equal to minus three tenths at two seconds, with interpolation computing every frame in between. Senior animators drew the key poses; assistants drew the in-betweens — literally called inbetweening — and the idea survives because sparsity is precisely what editability means. A two-second nod is three keys a human can see, grab, and move: shift the middle key later and the nod lands later, raise its value and the nod deepens. Fifty per-frame numbers have nothing a hand can hold. As Nami puts it, the route you can grab is the route you can fix. And her curves between waypoints are eased, never angular — ships have mass, and a route with corners was drawn by someone who never stood at a helm. Linear interpolation gets you there at constant velocity with a visible corner at every key, fine for mechanical values and wrong for bodies; eased curves accelerate and decelerate the way mass does, which is why every animation tool defaults to them. The one discipline that keeps this honest: easing is a property saved on the segment, because the editor two lessons from now must draw a curve that is exactly what the renderer will compute. If the curve you drag differs from the curve that renders, drag-to-fix quietly becomes drag-to-lie.",
      "bullets": [
        "A <b>keyframe</b> is a value pinned at a time (<code>head_pitch = -0.3 at t=2.0s</code>); <b>interpolation</b> computes every in-between frame.",
        "Sparsity IS editability: a 2-second nod is <b>three grabbable keys</b>, not fifty per-frame numbers. Move one and the nod reshapes.",
        "Interpolation is character: <b>linear</b> has a corner at every key (mechanical); <b>eased</b> curves accelerate like mass does — bodies get eased.",
        "Easing is saved <b>on the segment</b>, so the editor's curve is exactly what the renderer computes — else drag-to-fix becomes drag-to-lie."
      ]
    },
    {
      "heading": "The timeline: two layers with different jobs",
      "narration": "Nami's second discipline is the overlay sheets, and it maps onto the timeline's two layers, which must never be confused. The dense base layer is the master chart: the talking-head model's output — per-frame coefficient streams, twenty-five values a second per channel, for speech articulation and aliveness. It is machine-made and machine-scaled; no human will ever hand-edit frame one thousand eight hundred forty-seven of mouth-open, and that is fine, because it regenerates from audio whenever the speech changes. The sparse editable layer is everything drawn on the transparent sheets pinned over that master: gesture instances — a wave at two seconds, amplitude one point two, from the description lesson — and manual fixes, keyframes a creator drags into place. Human-made, human-scaled, all of it grabbable. The organizing structures are channels, one value over time like head pitch or brow raise, grouped into tracks, and clips. And here is the load-bearing choice: a gesture is a clip that owns its template keyframes. Drag the clip and the whole gesture moves; stretch it and it retimes; its amplitude stays attached to the clip rather than smeared across raw keys. At render time the composite is dense base, plus instantiated gesture clips, plus manual edit keys, merged with the additive-clamped rules the gesture lab already built. The two-layer split is exactly why an edited project survives regeneration — the property Nami prizes most. When the Log Pose resets and the base chart must be redrawn for a new island, every overlay sheet pins straight onto the new master; the corrections outlive the chart. Change the script, the dense layer re-renders from new audio, and every gesture and fix re-applies on top. Edits outlive the base they decorate.",
      "bullets": [
        "<b>Dense base layer</b>: the talking-head model's per-frame coefficient streams (25/sec) — machine-made, regenerated from audio, <b>never hand-edited</b>.",
        "<b>Sparse editable layer</b>: gesture clips (a wave at t=2.0, amp 1.2) and manual fix keyframes — human-made, human-scaled, <b>everything grabbable</b>.",
        "A gesture is a <b>clip that OWNS its template keys</b>: drag the clip and the whole gesture moves; stretch it and it retimes; amplitude stays attached.",
        "Because they're separate, edits <b>survive regeneration</b>: change the script, the dense base re-renders, every clip and fix re-applies on top."
      ]
    },
    {
      "heading": "Motion paths: when a value is a place",
      "narration": "Nami's apprentice supplies the third discipline by getting it wrong. Some motion is spatial: a pointing hand travels an arc across the frame, a gaze target sweeps from viewer to product, a head traces a small path. You represent that trajectory as a motion path — a parametric curve, typically Bézier segments: anchor points the path passes through, with handles shaping the curve between them. The creator will see it next lesson as a literal draggable line over the video; the data is just anchors and handles, each anchor optionally pinned to a time. And here is the trap every implementer falls into exactly once. The apprentice plots a curve through a strait and marks arrival times evenly along his pen strokes — but the curve's middle section, bent wide by his flourish, holds twice the actual distance of its ends. Stepping a Bézier's parameter t evenly, zero, zero point one, zero point two, does not advance evenly along the curve; segments where the handles stretch the curve pack more distance into the same parameter step. So his evenly-marked times have the ship inexplicably sprinting through the middle of the strait and crawling at the ends, for no visible reason. Nami's fix is a knotted string: she measures the curve's actual length — sample and sum, numerically — then maps time to distance traveled, so motion covers equal distance per equal time unless an easing curve deliberately says otherwise. Every animation library does this internally; you need to know it exists because the bug — why does the point speed up mid-path when I bend the curve — will otherwise cost you an afternoon. And because arc length is the bridge that lets timing and geometry become independently editable. The pen's pace is not the ship's pace. Measure the string, then hang the hours on it.",
      "bullets": [
        "Some motion is spatial: a pointing hand arcs across frame; a gaze sweeps. A <b>motion path</b> is a parametric curve — Bézier anchors + handles.",
        "The trap everyone hits once: <b>parameter is not distance</b>. Even t-steps (0, 0.1, 0.2…) do NOT advance evenly — stretched segments pack more distance.",
        "So constant \"parameter speed\" LOOKS like the hand rushing some stretches, crawling others — for no visible reason.",
        "Fix = <b>arc-length reparameterization</b>: measure the curve's real length, map time→distance. \"Measure the string, then hang the hours on it.\""
      ]
    },
    {
      "heading": "Non-destructive editing: the decision everything leans on",
      "narration": "This is the decision everything else leans on, and Nami's apprentice stages the disaster that proves it. The tempting shortcut: when the creator fixes something, write the fix into the coefficient data — mutate the base, save, done. It works for exactly one edit. Then undo requires remembering what was overwritten; two edits to the same region entangle; show-me-before-and-after is impossible; and the first script change regenerates the dense base, silently deleting every baked-in fix. The apprentice inks a correction directly onto a master chart, the next correction tangles with it, undoing means scraping ink, and Nami's verdict is quiet and total: you did not make an edit, you made a different chart — now nobody can say what the sea told us versus what you guessed. The professional pattern, which every serious editor and audio workstation and compositor converges on, is non-destructive editing. The generated base is immutable. Every edit is a delta record in an ordered edit list: channel, time range, keyframes of deltas, an enabled flag, and provenance — who made it, drag or LLM or template. Rendering composites base plus enabled edits, in order, and one detail carries enormous weight: edit keys store deltas, add minus fifteen hundredths to whatever is there, not absolute values. Deltas compose with the base's own motion and still mean the right thing when the base regenerates differently; absolute values fight the base and break the moment it changes. Corrections live on the transparent sheets, labeled with who drew them and why, stacked in order — remove a sheet and that correction vanishes cleanly, lift them all and the master is pristine underneath. Write on the plan and you die with the plan; write on the overlay and the plan can change beneath you.",
      "bullets": [
        "Tempting shortcut: write the fix INTO the base. Works for exactly ONE edit — then undo, A/B, and the next regeneration silently delete it.",
        "The professional pattern: base is <b>immutable</b>; every edit is an ordered <b>delta record</b> — channel, time range, delta keys, <code>enabled</code>, <b>provenance</b>.",
        "Rendering composites <b>base + enabled edits, in order</b>. The apprentice who inked the master \"did not make an edit — he made a different chart.\"",
        "Store DELTAS not absolutes: \"add -0.15 here\" survives a regenerated base; \"the value is 0.05\" bakes in a base that no longer exists."
      ]
    },
    {
      "heading": "Count what falls out free",
      "narration": "Now count what falls out free, because this is the payoff that makes the whole design worth its upfront cost. Undo and redo: pop and re-push the edit list — no state snapshots, no remembering what was overwritten, because nothing ever was. A-B comparison: toggle an edit's enabled flag and re-render the preview; the sticky note peels up and presses back down. Survival: regenerate the base — a new script, a better model — and re-apply the edit list on top, so fixes persist across regeneration, and that single property is what makes editing worth doing on generated content at all. When the venue floods and Monica reprints the entire master for the backup hall, the sticky notes peel off the old laminate and pin onto the new one, every correction intact. Attribution: when a creator asks why the head is tilted here, the answer is a named edit in a list — drag, or LLM, or template — not archaeology through baked-in numbers. And sync: the edit list is small, serializable, and diffable, so it is the project file itself, the thing Part Six's backend will store and version. Notice the shape of all five: with an ordered, toggleable, non-destructive edit list, undo is pop, A-B is a flag flip, survival is a re-composite, attribution is a lookup, and sync is a small diff — no dedicated machinery for any of them. Get the data model wrong and each of these is a separate hard feature you must build; get it right and they fall out free. The next two lessons are, honestly, just two input devices — a mouse and an LLM — writing records into the list this lesson designed. That is the correct amount of drama for a data model: all of it upfront, none of it later.",
      "bullets": [
        "<b>Undo/redo</b>: pop and re-push the edit list — no state snapshots. <b>A/B</b>: toggle an edit's <code>enabled</code> flag, re-render preview.",
        "<b>Survival</b>: regenerate the base, re-apply the list — fixes persist across regeneration, the property that makes editing generated content worth it.",
        "<b>Attribution</b>: \"why is the head tilted here?\" is a named edit in a list, not archaeology. <b>Sync</b>: the list is small and diffable — it IS the project file.",
        "The next two lessons are just <b>a mouse and an LLM</b> writing records into the list designed here. All the drama upfront, none of it later."
      ]
    }
  ]
};
