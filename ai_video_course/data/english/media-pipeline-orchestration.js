/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["media-pipeline-orchestration"] = {
  "id": "media-pipeline-orchestration",
  "title": "ffmpeg & Job Queues: Wiring Models Into One Reliable Media Pipeline",
  "titleNe": "Galley-La's Dock One: stamped parts, a foreman's board",
  "intro": "every stage exists — now they become one machine that survives contact with reality",
  "slides": [
    {
      "heading": "ffmpeg: the boring, load-bearing glue",
      "narration": "Water Seven's Galley-La Company builds ships no island rival can match, and a visiting Franky studies Dock One for a week to learn why. It is not the craftsmen, he concludes — it is the system. And the first part of that system is the least glamorous: between the models sits a mundane majority of the pipeline's actual work. Read whatever container the creator uploaded, extract its audio as sixteen-kilohertz mono WAV for Whisper and the speaker encoder, loudness-normalize a voice sample, turn four hundred rendered PNG frames plus a WAV into an MP4 people can play, burn in captions. All of it is ffmpeg — the open-source workhorse every video product you have ever used runs somewhere inside. You drive it as a subprocess with composed arguments: ffprobe for metadata, from which the intake gate's duration and sample-rate numbers come, and a frames-plus-voice invocation with codec flags for the final mux. It is deterministic, stateless, and fast — the pipeline's one component that never surprises you, which is exactly why everything routes through it rather than through per-model ad-hoc I-O. The mux deserves its own paragraph, because the talking-head lesson left a bomb armed: lip-sync quality is destroyed by a mux that offsets audio against frames. Every millisecond the sync model fought for, discarded by a wrong itsoffset or a frame-rate mismatch — twenty-five-fps coefficients muxed as twenty-four silently stretch the video four percent, and lips drift a full second by the two-minute mark. So assembly ends with a verification step, not a hope: probe the output's stream durations against each other and against the source audio, because drift shows up as a duration mismatch, and in CI, run the sync-expert scorer from the evaluation lesson on a sample. Galley-La's final inspector closes every build with a ritual the customers never see — mast against hull-line, measured, logged — because the dock once shipped a beautiful vessel whose mast sat two degrees off true. The chain of ceilings has a final link, and it is a command-line flag.",
      "bullets": [
        "Between the models is a mundane majority of the work: probe uploads, extract 16 kHz mono WAV, loudness-normalize, mux 400 PNGs + a WAV into an MP4, burn captions. All <b>ffmpeg</b>.",
        "Deterministic, stateless, fast — the one component that never surprises you. Driven as a subprocess: <code>ffprobe</code> for metadata, <code>-i frames/%04d.png -i voice.wav</code> for the mux.",
        "The talking-head lesson armed a bomb: a wrong <code>-itsoffset</code> or fps mismatch (25 coeffs muxed as 24 = <b>4% stretch, lips drift a full second by 2:00</b>) destroys sync.",
        "So assembly ends with <b>verification, not hope</b>: probe stream durations against each other and the source; in CI run the sync-expert scorer. The last link in the chain of ceilings is a CLI flag."
      ],
      "big": "25 fps muxed as 24 = 4% stretch = 1s lip drift by 2:00"
    },
    {
      "heading": "Jobs, not requests: the queue and the progress contract",
      "narration": "The foreman's board runs the whole yard: every commission a row, every station a column, chalk marks flowing left to right — queued, working, done, failed in red — and customers in the gallery can see exactly where their ship is: rigging, two-thirds strung. Franky notes it keeps them calm in a way we-are-working-on-it never could. That board is the job system. A full DenDen Studio render is minutes of GPU work, and nothing about that fits a request-response shape: the creator clicks Generate, and the system must accept immediately, work in the background, report progress honestly, survive the creator closing the tab, and allow cancellation. The architecture is the standard one — a job queue feeding worker processes — with media-specific decisions layered on. Jobs carry the validated plan, the stage list from Part One now made real. Workers claim one job at a time, because GPU stages do not share a card gracefully — one worker per GPU, with the model-warmth policy living inside the worker. Job state is a small machine: queued, running, then done or failed or cancelled, persisted, so a crashed worker's job is re-queueable rather than lost. The progress contract is product surface, not logging: workers emit stage-granular events — tts twelve of forty chunks, lipsync rendering three hundred of fifteen hundred frames — because a progress bar that says generating for four minutes reads as frozen, while stage three of five, sixty percent, reads as a machine working. Part Six's backend streams these events to the browser; here we just emit them. Cancellation is checked between stages and at safe checkpoints within long ones, because killing a worker mid-ffmpeg leaves temp files and mid-model-load leaves VRAM in doubt; a cancel flag polled at chunk boundaries costs nothing and exits clean. And the queue is where backpressure lives: one render at a time per GPU, a visible queue position for the rest — the honest version of the latency ladder, applied to batch. Customers who can see rigging two-thirds strung stay calm in a way we-are-working-on-it never achieves.",
      "bullets": [
        "A full render is minutes of GPU work — nothing about that fits request-response. Accept immediately, work in background, survive a closed tab, allow cancel.",
        "Standard architecture: a <b>job queue</b> feeding <b>workers</b>. Jobs carry the validated plan; one worker per GPU; job state = <code>queued→running→done|failed|cancelled</code>, persisted, so a crash is re-queueable.",
        "The <b>progress contract</b> is product surface: emit stage-granular events (\"tts: 12/40 chunks\") — \"generating…\" for 4 min reads as frozen; \"stage 3 of 5, 60%\" reads as a machine working.",
        "Cancellation checked <b>between stages</b> and at safe checkpoints — a cancel flag polled at chunk boundaries costs nothing and exits clean. The queue is where backpressure lives."
      ]
    },
    {
      "heading": "Stage contracts: artifacts in, artifacts out, hashes as truth",
      "narration": "Here is the discipline that makes everything else in this lesson cheap. At Galley-La, every station is a contract: the forge takes ore and a specification and produces a keel; the ribbing station takes the keel and lumber and produces a frame; final assembly takes everything and produces a ship. No station reaches into another's work; each declares what it needs and what it yields. Our stages are identical — every stage a function from declared input artifacts to declared output files, no hidden state. TTS consumes script-json plus a voice id and produces speech-wav; lip-sync consumes speech-wav plus source-png plus coeffs-json and produces frames; mux consumes frames plus speech-wav and produces final-mp4. Every part that leaves a station is stamped — what it is, what materials went in, which master's tools and which tool-generation made it — and the dock's ledger records every stamp. Our version is the manifest: each produced artifact content-hashed into the job's ledger — what exists, what it was made from, which model version made it. The manifest is not bureaucracy; it is the single structure that failure recovery, retries, caching, and debugging all read. Watch it earn its keep. Failure recovery: lip-sync crashes at frame nine hundred, the manifest shows speech-wav and coeffs-json intact and valid, so retry re-runs one stage, not the pipeline. When a rigging fire ruins a frame's ropework, the foreman consults the ledger, sees the keel's stamp and the frame's stamp intact and matching spec, and orders only rigging redone — the old yards would reforge the keel after any failure, but we do not guess about what is good. Idempotent retries: a stage writes to a temp path and renames into place on success, atomic on the same filesystem, so a stage that died mid-write leaves no half-artifact the manifest could mistake for done. Half-finished work never enters the ledger — a station completes a part on its bench and only then carries it to the stamped racks. Completion is defined by the manifest entry, not by file existence, which is what makes re-running always safe. And debugging: why does this render sound wrong starts with the manifest — which voice, which model version, which inputs — the provenance discipline from the edit list, applied to heavyweight artifacts.",
      "bullets": [
        "Every stage is a <b>contract</b>: declared inputs (artifact names), declared outputs (artifact files), no hidden state. TTS: <code>script.json</code>+voice → <code>speech.wav</code>; mux: <code>frames/</code>+<code>speech.wav</code> → <code>final.mp4</code>.",
        "Each artifact is content-hashed into a <b>manifest</b> — the job's ledger: what exists, what it was made from, which model version made it. Failure recovery, retries, caching, debugging all read it.",
        "<b>Resume-from-last-artifact</b>: lip-sync crashes at frame 900 → manifest shows speech.wav + coeffs.json intact → retry re-runs ONE stage. Nobody reforges the keel.",
        "<b>Atomic completion</b>: write to a temp path, rename on success. \"Done\" = a manifest entry, never \"file exists\" — so a mid-write death leaves no half-artifact to mistake for done."
      ]
    },
    {
      "heading": "Caching at pipeline scale: the chunk cache, grown up",
      "narration": "The TTS lesson cached chunks by content key; the same move now covers every stage. A stage's cache key is the hash of three things: its input artifacts' content hashes — not their names, their content — the model name and version, and the parameters that change output. Before running a stage, look up the key; on a hit, link the cached artifact into the job and skip the work entirely. The stamps pay this dividend at Galley-La: when two commissions specify identical dinghies, the second one's hull simply already exists — same specification, same materials, same tool-generation, same stamp — and the yard shelves one hull for both. Bernadette's line says it plainer: two hospital orders for the same formulation from the same API batch draw from the same validated lot, because making it twice would just be making it once, twice. The properties compound across the product: a creator re-renders after fixing one gesture, the audio inputs are unchanged, so TTS and voice stages hit cache and only lip-sync onward re-runs; two projects using the same preset voice for the same sentence share the artifact; a model upgrade changes the version component and produces honest misses, no stale audio. But watch what the key composition does automatically: change one input and invalidation cascades exactly as far as it should. New script, new script hash, TTS key changes, new speech-wav hash, lip-sync key changes, everything downstream re-runs. But change the output-resolution parameter of the mux and only the mux re-runs, because the upstream keys never saw that parameter. Nobody writes invalidation logic — the dependency graph is the invalidation logic, encoded in hashes. This is the machinery the LLM director drives next lesson: it emits a full plan every time, and execution costs only what actually changed — plan boldly, pay marginally. Two disciplines keep it sound: parameters must be canonicalized before hashing — the rounding lesson from TTS keys — and every parameter that affects output must be in the key. The one you forget is the one that serves someone else's render.",
      "bullets": [
        "A stage's <b>cache key</b> = hash of its input artifacts' <b>content hashes</b> + model name AND version + the parameters that change output. On hit, link the cached artifact and skip the work.",
        "Change ONE input and invalidation <b>cascades exactly as far as it should</b>: new script → new script hash → TTS key changes → speech.wav changes → downstream re-runs. Change mux resolution → only mux re-runs.",
        "Nobody writes invalidation logic — <b>the dependency graph IS the invalidation logic</b>, encoded in hashes. Two hospital orders, same formulation → the second draws the same validated lot.",
        "Two disciplines keep it sound: <b>canonicalize params before hashing</b>, and every output-affecting parameter must be IN the key — the one you forget serves someone else's render."
      ]
    },
    {
      "heading": "The last station verifies (what carries forward)",
      "narration": "Step back and the architecture is recognizable. Stages with declared inputs and outputs, content-addressed artifacts, cascading invalidation, resumable partial builds — this is a build system, Bazel's action graph or Nix's derivations or make with content hashes instead of mtimes, and separately a workflow engine, an Airflow or Temporal DAG with persisted state and retries. Knowing the family pays twice. First, you inherit its lessons wholesale. Hermeticity: a stage that reads anything outside its declared inputs — an environment variable, the system clock, a global config — breaks cache correctness in ways that surface as stale outputs weeks later; the output changes, the key does not, and someone files the least-reproducible bug of the quarter. Declared inputs are the whole input surface, enforced in review — an aged craftsman tells Franky the old yards would reforge the keel to be safe, but Galley-La does not guess, because the stamp says what a part was made from and nothing outside it. Reproducibility is a spectrum: bit-identical for ffmpeg and pinned-seed models, approximately-identical acceptable for GPU float nondeterminism — which is exactly why the stage key hashes inputs rather than comparing outputs, so benign re-runs do not spuriously cascade. And you split small artifacts from big: manifests and coefficients in the database, frames and WAVs on disk or object storage with hashes in the manifest. Second, you know when to stop hand-rolling: a single-machine product with five stages is well-served by two hundred lines of the worker loop, but the day you need multi-machine scheduling and complex fan-out DAGs, adopting Temporal-class machinery beats growing a bespoke distributed system — and the contracts you wrote are exactly what makes migration cheap, because pure functions over declared artifacts port anywhere. Above all, verify at the seams where errors become invisible to components but visible to users — and no seam matters more than final assembly, because the mux is the last transformation before human eyes. Every-render checks stay cheap and catastrophic: stream-duration comparison, frame-count-versus-audio arithmetic, one ffprobe call. CI-only checks are the expensive ones: the sync scorer on sampled windows, a two-minute-plus fixture because linear drift is invisible on short clips, golden-render comparisons. Bernadette's catchphrase is posted above the door, and it is the whole lesson: the last station's mistake is the only one the customer meets — so the last station verifies, mechanically, every single time.",
      "bullets": [
        "You've reinvented a <b>build system</b> (content-addressed artifacts, cascading invalidation, resumable builds) fused with a <b>workflow engine</b> (persisted DAG, retries) — on purpose. Inherit their lessons: <b>hermeticity</b>, reproducibility, small-vs-big artifact split.",
        "<b>Hermeticity</b>: a stage reading anything outside its declared inputs (env var, clock, global config) poisons the cache invisibly — the output changes, the key doesn't. Declared inputs are the whole input surface.",
        "Verify at the <b>seams where errors become invisible to components but visible to users</b> — above all at assembly. Every-render: duration + frame-count checks (one ffprobe). CI: sync scorer, a 2-min fixture, golden renders.",
        "\"The last station's error is the only one the sea gets to find.\" Verification proportional to finality: the inspector measures the mast on every ship, not the prototype."
      ]
    }
  ]
};
