/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["hardware-quantization-caching"] = {
  "id": "hardware-quantization-caching",
  "title": "Fitting It All: VRAM Budgets, Quantization Tradeoffs & Caching Renders",
  "titleNe": "Provisioning the Sunny: manifest, compressed rations, warehouse rule",
  "intro": "the whole product must fit on one machine — this is where the open-source promise gets kept",
  "slides": [
    {
      "heading": "The ledger becomes policy: residency plans per mode",
      "narration": "Before the crew's longest crossing, the Sunny becomes a lesson in fitting. Nami runs the manifest with a shipwright's coldness: everything that boards gets weighed — not catalog weights, actual scale weights, because the catalog does not know how much water your rope drinks — and the hold's capacity is a number on the wall, with a red margin line above it that no one is allowed to plan into. Part Zero taught the arithmetic — co-residency costs the sum, sequencing costs the max plus load latency — and the shipping product needs that arithmetic turned into named policies. DenDen Studio has three modes with different working sets, just as Nami's stowage is not one plan but three. Edit mode: the director LLM and the VLM should answer in under a second, so keep the LLM warm always because it is the interaction loop, the VLM warm during active sessions, media models cold. Batch render: the worker runs stages sequentially, load, run, release, keeping at most the two models the plan uses back-to-back — a six-second model load amortizes fine over a ninety-second frames stage and terribly over a two-second mux. Live mode: the co-residency sum, small variants, everything warm, nothing else allowed on the card. The mechanism under all three is the same: a model manager owning load and unload with LRU-by-usage and per-mode pin lists — the worker asks for a model, the manager loads it, evicting the least-recently-used unpinned model if the ledger would overflow. Two disciplines keep it honest. The ledger's numbers are measured — load the model, read the allocator, write it down, because spec sheets lie by the working-memory margin. And mode transitions are explicit events: Nami's battle mode is a pre-drawn list executed before the enemy closes, because re-stowing during a broadside is how ships die. Entering live mode evicts to the live pin list before the session starts, not when the first over-budget allocation crashes mid-performance.",
      "bullets": [
        "Part 0's arithmetic (co-residency = sum, sequencing = max + load latency) turned into <b>named policies</b>. Three modes, different working sets.",
        "<b>Edit</b>: LLM warm always (the interaction loop), VLM warm during sessions, media cold. <b>Batch</b>: sequential load-run-release, keep the two back-to-back models. <b>Live</b>: co-residency sum, small variants, everything warm, nothing else on the card.",
        "A <b>model manager</b> owns load/unload: LRU-by-usage + per-mode pin lists, evicting the least-recently-used unpinned model on overflow.",
        "Two disciplines: the ledger's numbers are <b>MEASURED</b> (spec sheets lie by the working-memory margin), and mode transitions are <b>explicit events</b> — evict BEFORE the mode starts, not mid-crash."
      ]
    },
    {
      "heading": "Quantization: what the bits buy, and who pays",
      "narration": "Sanji's contribution is compression, applied with a chef's discrimination. Rice, beans, stock bases — dried and pressed to a quarter volume, and in a stew you will never know. But the crossing's single celebration dinner travels full — the fresh cut, the real butter — because compression is invisible in the stew and criminal in the centerpiece. You compress by dish, not by policy. That is quantization. Mechanically, in one breath: weights trained at sixteen-bit float are stored in four to eight bits with per-block scale factors; memory drops proportionally, and inference actually speeds up on memory-bandwidth-bound hardware, because moving weights, not multiplying them, is the bottleneck. The uneven part — the part that makes this an engineering decision rather than a checkbox — is who pays quality. The LLM director pays least: schema-constrained planning at temperature zero is robust at Q4, because the constrained decoder masks most of the degradation surface, which is why quantize-the-text-brain-hardest is the default. Whisper at int8 is a solved trade, accuracy loss within noise for clean audio. TTS and voice conversion pay audibly earlier: quantization artifacts land in exactly the high-frequency texture the chain of ceilings runs through — a slightly grainy voice becomes slightly mushier lips downstream. And diffusion and rendering models pay most visibly: aggressive quantization shows up as texture smearing and detail loss in the one artifact the creator stares at. So the rule is not a rule but a procedure: golden sets per stage. A pinned set of reference inputs per model, reference outputs from the full-precision build, and per-stage comparison metrics — plan-property assertions for the director, the evaluation lesson's sync and quality scores for media. Sanji's proof is not opinion: before departure he cooks both versions of every compressed candidate side by side, the crew tastes blind, and anything the tasting flags travels whole. A quantization candidate ships when its golden run is within threshold — and the same harness gates model upgrades, making new-quant, new-model, new-version one decision pipeline instead of three vibes. Never ask does it seem fine; ask what is the measured delta on the pinned set. Nobody vibes.",
      "bullets": [
        "Mechanics: 16-bit weights stored in 4-8 bits with per-block scales; memory drops proportionally, inference SPEEDS UP (moving weights, not multiplying, is the bottleneck).",
        "The uneven tax — who pays quality: <b>LLM director</b> pays least (Q4 robust — the constrained decoder masks degradation). <b>Whisper</b> int8 is solved. <b>TTS/VC</b> pay audibly earlier (grainy voice → mushier lips). <b>Diffusion/render</b> pays most visibly (texture smearing).",
        "So it's a procedure, not a rule: <b>golden sets per stage</b> — pinned reference inputs, full-precision reference outputs, per-stage comparison metrics.",
        "A candidate ships when its golden run is within threshold — and the same harness gates model UPGRADES. \"Compression is invisible in the stew and criminal in the centerpiece.\""
      ]
    },
    {
      "heading": "Profiling: find the minutes before spending them",
      "narration": "The orchestration lesson's stage events are also the profiler: every render logs per-stage wall time, and the aggregate answers the only question that matters before optimizing — where do the minutes go? For talking-head pipelines the answer is nearly always the frames stage: per-frame neural rendering dominates, TTS and coefficients are seconds, mux is nothing. That immediately ranks the levers. Resolution is the big one: rendering at five-forty-p versus ten-eighty-p is roughly a four-times pixel difference in the dominant stage, and the preview-final split falls out naturally — edit-loop previews render at five-forty-p, the approved final at full resolution, so the creator iterates fast and ships pretty, the drag lesson's proxy-fidelity idea at pipeline scale. Chunked rendering, the frames stage processing in segments, bounds memory and enables the resume behavior the worker already supports. Precision, fp16 versus fp32 inference, is usually free speed on modern GPUs. And the director belongs on the CPU: llama.cpp runs an eight-B Q4 plan-emission in seconds on a desktop CPU, freeing the entire GPU for media — the rare case where slower hardware is the right architecture, because planning latency is off the render's critical path. The discipline attached: profile before and after every lever pull, on the same golden inputs, and keep the history — because optimizations interact. Chunking changed the frames stage's memory profile, which changed what could stay resident, which changed load-latency totals. And because the numbers rot as models upgrade. A pipeline that logs stage timings has this discipline nearly free; the only added ceremony is the before-and-after comparison being a habit instead of an accident.",
      "bullets": [
        "The orchestration lesson's stage events ARE the profiler: every render logs per-stage wall time. The only question before optimizing — <b>where do the minutes go?</b>",
        "For talking-head pipelines it's <b>nearly always frames</b> (per-frame neural rendering; TTS + coeffs are seconds, mux is nothing). That ranks the levers.",
        "<b>Resolution</b> is the big one: 540p vs 1080p ≈ 4x pixels in the dominant stage → the preview/final split (540p edit loop, full-res approved). <b>Chunking</b> bounds memory + enables resume. <b>fp16</b> is usually free.",
        "The <b>director belongs on the CPU</b>: llama.cpp emits an 8B Q4 plan in seconds, freeing the whole GPU for media — \"slower\" hardware as the right architecture (planning is off the render's critical path)."
      ]
    },
    {
      "heading": "Cache economics: disks fill, so eviction is a policy",
      "narration": "Content-addressed caching made re-renders cheap; nothing made the cache small. Frames directories are hundreds of megabytes, WAVs add up, and a month of iterating creators fills any disk. Eviction is therefore designed, not discovered. At the crossing's staging island, the crew inherits the harbor warehouse's eviction rule from its ancient keeper: the warehouse fills every season, so what leaves is decided by a posted policy. Our policy has three parts. A size budget, a config value with a dashboard number behind it. LRU by last-use as the base policy, and the manifest already records access, because cache hits are uses. And pins for what must not vanish: artifacts of currently-open projects, and anything the consent or audit trail requires — enrollment artifacts are records, not cache, so they live outside the eviction domain entirely. Those are not storage, the keeper says of the pinned board; those are promises. But the refinement worth its complexity is this: evict by regeneration cost per megabyte, not just recency. A speaker embedding is kilobytes and cost a model load to make; coefficients are small and cost a minute; frames are huge and cost the most wall time but are also purely derivable from coefficients plus image, cheap-ish to remake at need; the final mux is derivable in seconds. So the policy keeps small expensive intermediates aggressively — embeddings, coefficients, TTS chunks — lets bulky rederivable artifacts go first, and never evicts below the active-project pin set. A barrel of fish I can throw out, the keeper says; the sea makes more fish. A surveyed chart of the reef passage took three men a month — that stays if I must sleep outside myself. You evict by what it costs to remake, not by what it costs to keep. This is also the answer to what we sync to the cloud in Part Six: the small expensive layer travels; the bulky derivable layer regenerates locally. One more echo of the course's oldest split — the sparse valuable representation versus the dense derivable one, the edit list versus the render, now as storage policy.",
      "bullets": [
        "Content-addressed caching made re-renders cheap; nothing made the cache small. Frames dirs are hundreds of MB; a month of iterating fills any disk. Eviction is <b>designed, not discovered</b>.",
        "Base policy: a <b>size budget</b> (config + dashboard number), <b>LRU by last-use</b> (cache hits ARE uses), and <b>pins</b> for active projects + audit artifacts (enrollment records live outside the eviction domain).",
        "The refinement: evict by <b>regeneration cost per megabyte</b>, not just recency. Keep small expensive intermediates (embeddings, coefficients); let bulky rederivable artifacts (frames, muxes) go first.",
        "\"You evict by what it costs to REMAKE, not what it costs to keep.\" This is also the cloud-sync answer: the small expensive layer travels, the bulky derivable layer regenerates. The sparse-valuable vs dense-derivable split, as storage."
      ],
      "big": "Evict by cost-to-REMAKE, not cost-to-keep"
    },
    {
      "heading": "What carries forward",
      "narration": "Step back and the whole fitting problem is one discipline applied three times: a measured budget, plus a written policy, plus an instrumented mechanism — at VRAM, at GPU-time, and at disk. Two levels down, the quantization details reward knowing. The llama.cpp K-quants use super-blocks with two-level scaling and mixed precision across tensor types — attention and output layers, measured as more sensitive, get more bits, the feed-forward bulk gets fewer, and the S, M, L suffixes trade that mix. Importance-aware variants run calibration data through the full model to spend the bit budget where it matters, meaningfully better below Q4 and marginal above it. And the non-weight gigabytes — the ledger's working-memory line — unpack into the KV cache, roughly half a megabyte per token of context for an eight-B model, so a four-thousand-token grounding block holds about two gigabytes on top of weights, and the activations, which for rendering models scale with resolution squared, which is exactly why the OOM fix clamps inputs at intake. The headroom line in every budget is these, given a number, and it is one and a half gigabytes rather than a hundred and fifty megabytes precisely because three of the four are input-shaped and only reveal their maximum on the worst upload a creator will eventually send. Finally, the product-review question: why does our app need twelve gigabytes when a competitor runs in the browser? The honest answer is that the competitor's models run on their servers — the VRAM did not disappear, it moved to a datacenter and onto a subscription bill, along with the user's face and voice biometrics; our local-first architecture is a privacy feature precisely because the twelve gigabytes is the user's own. And the number is negotiable, with prices: the director already runs on CPU, deeper quantization trades golden-set-gated quality, a batch-only tier sequences stages in about six gigabytes. The right question is not why twelve but which tier of hardware we serve at which quality — and the ledger, the golden sets, and the residency policies are exactly the machinery that lets us answer that as a product decision with measured tradeoffs, instead of a defensive number.",
      "bullets": [
        "Fitting a 5-model pipeline on one GPU is <b>the same discipline three times</b>: a measured budget + a written policy + an instrumented mechanism — at VRAM, GPU-time, and disk.",
        "Deeper: K-quants use super-blocks + mixed precision (sensitive layers get more bits); non-weight GB live in the <b>KV cache</b> (~0.5 MB/token at 8B fp16) and <b>activations</b> (scale with resolution²) — the headroom line is input-shaped.",
        "\"Why 12 GB when [competitor] runs in the browser?\" — their VRAM moved to a datacenter and a subscription, with the user's face + voice. Local-first IS the privacy feature. The number is measured, negotiable, and tiered.",
        "The through-line: budgets, golden sets, and residency policies are the machinery that lets \"which hardware tier at which quality\" be a <b>product decision with measured tradeoffs</b>, not a defensive number."
      ]
    }
  ]
};
