/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["click-drag-correction"] = {
  "id": "click-drag-correction",
  "title": "Click-Drag Fixing: Direct Manipulation of Generated Motion on a Canvas",
  "titleNe": "Franky's guide-handles: grab the arm, the rig writes the fix",
  "intro": "the four-promise contract of direct manipulation — and the transform stack where every drag bug is born",
  "slides": [
    {
      "heading": "The contract: what \"direct\" promises",
      "narration": "The portrait theater's third act opens with a communication breakdown. The mayor keeps requesting fixes in words — the pointing should be more toward the harbor, and prouder, but not so much — and after four rounds of Usopp adjusting rod-numbers and the mayor squinting, Franky rebuilds the interface overnight. His invention is guide-handles: every adjustable rod now ends in a brass grip the mayor can simply take, move the painted arm where he wants it, and the rig writes the correction automatically. That is direct manipulation, and it earns its name through four promises. Visible equals grabbable: whatever the creator can see wrong — a keyframe dot, a motion-path anchor, a clip's edge — must be the literal thing the pointer grabs, with no select-the-object-then-find-its-dialog-then-locate-the-field indirection. Immediate feedback: the dragged thing follows the pointer and the consequence previews live — the portrait's pose changes as the mayor moves it — because past about a hundred milliseconds of lag the object stops feeling held and starts feeling remote-controlled. Constraints act during the drag: the rods stiffen at the joints' safe limits, so pulling the arm past where a shoulder bends simply stops, firmly and wordlessly. As Franky says, the rig argues with your hand, not with your ears — joint limits and snaps and axis locks shape the motion as it happens, instead of letting you place an illegal value and scolding you after. And release commits exactly one thing: only when the mayor lets go does the rig ink one clean correction onto the overlay — his whole adjustment as a single mark, which Nami can peel off whole. One grab, one mark, one regret. In DenDen Studio these promises play out across three views — a curve view, a spatial view over the video, and a timeline of clips — but all three write the same records the LLM will write next lesson. That convergence was the design test, and this lesson is where it pays.",
      "bullets": [
        "<b>Visible = grabbable</b>: the wrong thing you SEE is the thing your pointer grabs — no \"select object, open dialog, find numeric field\" detour.",
        "<b>Immediate feedback</b>: the consequence previews live; past ~100 ms lag it stops feeling held and starts feeling remote-controlled.",
        "<b>Constraints act DURING</b>: joint limits, snaps, axis locks shape the drag as it happens — the handle stiffens at a wall, it doesn't scold you after.",
        "<b>Release commits ONE thing</b>: mouse-up = one edit record; undo removes the whole gesture, not forty pixel-increments. \"One grab, one mark, one regret.\""
      ]
    },
    {
      "heading": "Coordinates: where drag bugs are born",
      "narration": "Underneath the feel is a stack of coordinate transforms, and this is where almost every drag bug is born. A pointer event arrives in screen pixels. Transform one: the canvas may be CSS-scaled and sits at an offset, so subtract the canvas origin and scale by devicePixelRatio — retina displays render the canvas at two or three times its CSS size, and if you forget this, every hit lands half an inch off; that is the second-most classic bug. Transform two: the video preview is drawn letterboxed inside the canvas, so map into video-frame coordinates, the space where landmarks and motion paths live — deliberately the same pixels the intake detector already uses. For the curve view, transform three maps pixels to domain values: x to time, y to channel value. And here lives the classic bug of them all. Screen Y grows downward while every value axis grows upward, so the mapping must invert — and Franky learned this the hard way when his apprentice mounted the overlay scroll behind the translucent master instead of in front, reading every correction mirrored: the mayor dragged the arm toward the harbor and the recorded fix moved it toward the tavern. One flipped bracket later, Franky adds a ritual to every setup — grab a test handle, drag it up, confirm the mark goes up — every view, day one, no exceptions. The discipline that keeps the whole stack sane: write each transform as a pure function with its inverse, screen-to-value paired with value-to-screen, compose them rather than inlining arithmetic in event handlers, and round-trip test that inverse of forward of a point returns the point, at devicePixelRatio one and two, zoomed and scrolled. Ten lines of test catch the entire bug class, because when a fix lands somewhere else, the answer is always one of these functions. Every haunted rig Franky ever fixed was a mirror somewhere in the linkage.",
      "bullets": [
        "A pointer event arrives in <b>screen pixels</b>. Transform 1: subtract canvas origin, scale by <code>devicePixelRatio</code> (retina renders 2-3x — forget it and every hit lands off).",
        "Transform 2: into <b>video-frame coords</b> (same pixels the intake detector uses). Transform 3 (curve view): x→time, y→value.",
        "THE classic bug: screen Y grows <b>down</b>, value grows <b>up</b> — the mapping must invert. Smoke test: <b>drag up, confirm up, every view, day one</b>.",
        "Discipline: each transform is a pure function <b>with its inverse</b>, round-trip tested. \"Every haunted rig has a mirror somewhere in the linkage.\""
      ]
    },
    {
      "heading": "Hit-testing & affordances",
      "narration": "Franky's engineering hides in the feel, starting with hit-testing. A keyframe dot renders at four pixels, and nobody can reliably click a four-pixel target; on touch, a fingertip is forty CSS pixels of ambiguity. So hit-testing is nearest-within-radius: measure the distance from the pointer to every candidate — in the candidate's own coordinate space, post-transform, because testing screen-space pointer against untransformed positions is a bug all its own — accept the nearest within a tolerance of eight to twelve pixels for a pointer, twenty or more for touch. Franky's grips are fat for exactly this reason: harbor hands miss thin handles. When candidates overlap, break ties by priority. The already-selected object wins, so a fine adjustment near a neighbor does not steal the grab — when two grips sit close, the rig favors the one the mayor grabbed last, so tuning the wrist never accidentally grabs the elbow. Then smaller, harder targets beat larger ones: a keyframe dot beats the curve segment under it, the curve beats the background pan. And for paths and curves, distance is point-to-segment, not point-to-anchor, because creators grab the line between keys expecting to bend it. Affordances close the loop before any click. Hover swells the dot and changes the cursor, so grabbability is advertised, not discovered by trial. The grabbed object visibly attaches — color, a slight scale. A ghost of the original position stays faintly visible during the drag, so the mayor sees before and after in one glance — the A-B instinct, live, exactly the chalk ghost Franky's rig holds beside the moved arm. And constraint proximity telegraphs: the snap target glows as you approach, the clamp boundary draws as a subtle wall. None of this is decoration; every affordance answers a question the creator would otherwise answer by clicking wrong — what can I grab, what am I holding, where was it, and what happens if I release.",
      "bullets": [
        "A 4 px dot is unclickable; a fingertip is ~40 css-px of ambiguity. So: <b>nearest-within-radius</b> (8-12 px pointer, 20+ touch), measured POST-transform.",
        "On overlap, break ties by <b>priority</b>: the already-selected object wins (fine adjustment doesn't steal a neighbor), then smaller/harder targets over larger.",
        "Distance to curves is <b>point-to-SEGMENT</b>, not point-to-anchor — creators grab the line between keys to bend it, and that grab should work.",
        "Affordances advertise: hover swells the dot, the grab visibly attaches, a faint <b>ghost</b> of the original stays (live A/B), snaps glow as you approach."
      ]
    },
    {
      "heading": "The drag loop: pixels to one clean edit",
      "narration": "The drag loop is a small state machine, and every commitment in it matters. Press: hit-test, and on a hit record the anchor — pointer position, the object's original value, a timestamp — then capture the pointer, because drags continue outside the canvas and losing the object at the window edge is amateur hour. Move: transform pointer to domain space, then apply constraints in domain space — clamp to the channel's limits, the same low and high the render composite enforces, so the drag can never even propose what the renderer would reject; axis-lock if shift is held; and snap to frame boundaries or neighbor keys within a snap radius, with snapping applied after clamping, because snapping to a target outside the legal range would un-clamp. Then update the provisional value and request a preview recomposite — throttled to animation frames, recomputing only the affected channel over only the affected time range, the data model's sparsity making the under-a-hundred-milliseconds promise affordable. Release: discard the provisional state and write one edit record — channel, delta keys of new minus original, a default easing, provenance drag — through the same API the LLM editor will use next lesson. Escape cancels for free: the provisional state vanishes and nothing was ever written. Two commitments deserve emphasis. Provisional-until-release keeps the edit list clean — no forty-records-per-drag pollution, no undo-undoes-one-pixel, and cancel is free because nothing was committed. And constraints-in-domain-space means the physics is real: the drag literally cannot exceed a joint limit, because the transform clamps before the preview ever sees the value — the handle visibly stiffens at the wall, teaching the limit by feel. Franky's rehearsal-proven poses are cut as shallow detents into the guide tracks: near a detent the handle tugs gently and clicks home, and holding against the pull glides past it for the times the mayor truly wants between. Compare the alternative everyone builds once — free drag, validate on release, error toast, value out of range. Same rule delivered as rejection instead of physics, and that single choice, repeated everywhere, is the difference between a tool that feels like clay and a tool that feels like a form with a strict clerk.",
      "bullets": [
        "<b>Press</b>: hit-test; record the anchor (pointer, original value, timestamp); <b>capture the pointer</b> so drags survive leaving the canvas.",
        "<b>Move</b>: transform to domain; <b>clamp FIRST, then snap</b> (snap-then-clamp would un-clamp); update provisional value; request throttled, affected-scope-only preview.",
        "<b>Release</b>: write ONE edit record — channel, delta keys, easing, <code>provenance:'drag'</code> — through the <b>same API the LLM editor uses</b>.",
        "<b>Escape</b> cancels free: provisional state vanishes, nothing was ever written. Constraints as physics > free-drag-then-\"value out of range\" toast."
      ]
    },
    {
      "heading": "What carries forward",
      "narration": "Step back and see what carries forward. Direct manipulation is a discipline, not a widget: the visible thing is the grabbable thing, feedback lands within a perceptual deadline of about a hundred milliseconds, constraints act during the drag as physics rather than after it as error messages, and release commits exactly one undoable record. Miss any one promise and the tool stops feeling like clay. The bugs concentrate in two places, and knowing that makes you fast. The transform stack is the first — inverted Y and unscaled devicePixelRatio are the classics — caught by round-trip tests and the drag-up-confirm-up ritual taped to the monitor. Hit-testing is the second — testing in the wrong space, or missing priority so the easy big target steals from the small hard one that was almost always the intent — caught by a debug overlay that renders every hit region and the transformed pointer live, because hit bugs are invisible in code review and obvious on screen. When the preview cannot keep up, degrade fidelity before responsiveness: freeze the video frame and animate only the overlay — the curves, the path, the landmarks — because a laggy direct manipulation is worse than an honest indirect one; the hand corrects against stale feedback and oscillates. That is the live pipeline's degradation ladder, transplanted into the editor: fixed responsiveness, negotiable fidelity. And the payoff that ties Part Four together: the mouse and the LLM are two input devices emitting identical records into the same edit list. Undo, A-B, provenance, and survival-across-regeneration all come from the data model, not from either device. Howard finally conceded that every haunted robot has a mirror in it somewhere; Franky told his apprentice the same. Get the transforms honest and the records uniform, and the next lesson — teaching an LLM to write into this exact list — is one more hand on the same clay.",
      "bullets": [
        "Direct manipulation is a <b>discipline, not a widget</b>: visible=grabbable, feedback under ~100 ms, constraints as physics, one release = one record.",
        "Bugs concentrate in <b>two places</b>: the transform stack (inverted Y, unscaled DPR) and hit-testing (wrong space, no priority) — both caught by cheap tests.",
        "Degrade preview fidelity before responsiveness: freeze the video, animate the overlay only — a laggy \"direct\" tool is worse than an honest indirect one.",
        "The convergence pays: the <b>mouse and the LLM are two input devices</b> emitting identical records. Undo, A/B, provenance come from the data model, not the device."
      ]
    }
  ]
};
