/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["llm-as-director"] = {
  "id": "llm-as-director",
  "title": "The LLM as Director: One Description In, a Whole Pipeline Plan Out",
  "titleNe": "Iceburg's desk: one sentence in, a work order out",
  "intro": "the course's central promise assembled — the most capable component, and the least trusted, by design",
  "slides": [
    {
      "heading": "The plan, full size: one document the whole product speaks",
      "narration": "A merchant walks into Galley-La with one sentence: I want a ship that feels fast but homey, my family sails with me. Iceburg listens, and what happens next is why Dock One's president is Dock One's president — he turns that sentence into a complete work order, in the yard's own vocabulary, every station's parameters named. That is the director pattern, and it is the course's central promise assembled. The creator writes one paragraph, and the local LLM produces the complete generation plan — stages, voice, gestures, ambient motion, parameters — as one document. Every schema this course has built merges into this one: voice with id and mode, the stage list, gestures with time and amplitude and hand, an ambient prompt, per-stage parameters, and an unmatched channel for honesty. The director — your pinned, temperature-zero local LLM — reads the creator's paragraph plus the grounding block and emits this document under a JSON schema whose enums are the real registries: stage names from the executor's contracts, gestures from the library, voices resolved against the consent registry, channels from the editable set. Stations Iceburg names exist; parts he cites carry real stamps; nothing is invented. Constrained decoding makes off-registry references unpickable, and the validator makes off-reality references un-executable — the same two-wall defense as always, now guarding the whole production. But the plan is more than a request format: it is the product's lingua franca. The editing UI's state is a plan being mutated — a dragged gesture edits gestures-two's amplitude. The executor consumes plans. The cache keys derive from plan content. A project's history is a sequence of plans, diffable like the edit list. One document type flowing through every subsystem is what keeps a five-model pipeline from becoming five products — and it is why the director can be swapped, upgraded, or bypassed entirely, a power user editing the plan JSON directly being a perfectly legitimate client, without anything downstream noticing.",
      "bullets": [
        "Every schema merges into one: <code>{voice, stages, gestures, ambient, params, unmatched}</code>. The temperature-0 director reads the paragraph + grounding and emits it under a JSON schema whose enums are the <b>REAL registries</b>.",
        "Two-wall defense: constrained decoding makes off-registry references <b>unpickable</b>; the validator makes off-reality references <b>un-executable</b>.",
        "The plan is the product's <b>lingua franca</b>: the editing UI mutates a plan, the executor consumes plans, cache keys derive from plan content, history is a sequence of plans.",
        "One document type through every subsystem is what keeps a 5-model pipeline from becoming 5 products — and lets the director be swapped, upgraded, or bypassed (editing plan JSON directly is a legit client)."
      ]
    },
    {
      "heading": "Grounding: plans cite facts, or they are fiction",
      "narration": "Iceburg does not guess. He sends a runner to measure the merchant's current ship, consults the registry of the family's berth licenses, checks the yard's ledger for reusable stamped parts, and only then writes the work order. A director that guesses clip durations, invents voice ids, or assumes an upload's properties produces plans that fail validation at best and burn GPU at worst. So the director works like the edit session did: compact always-relevant state inline — project duration, enrolled voices with ids, the gesture enum, the current plan if revising — and read-tools for what must be probed fresh. Probe-media is the ffprobe wrapper returning duration, sample rate, resolution. Get-intake-report returns the multimodal lesson's verdict: subject type, orientation, obstructions. Estimate-stage returns predicted seconds and VRAM from the hardware lesson's tables — the bill the creator will see. And check-cache reports whether a stage would hit, letting the director tell the creator audio unchanged, reusing it. The loop runs with Part One's full discipline: iteration cap, registry-checked calls, validated arguments. A well-grounded director's behavior is visibly different in logs — it probes the upload before choosing the talking-head path, so a profile shot makes it plan the warning the intake lesson designed; it lists voices before assigning one, never inventing an id; it checks cache before estimating, so the preview says roughly forty seconds, voice and coefficients reused, instead of scaring the creator with the full-render number. The merchant never sees the order's internals — he sees Iceburg's summary sheet: schooner hull, family cabins, the fast rig, six days, two days saved because your keel dimensions match a stamped hull we hold. Grounding is not politeness; it is the difference between a plan that describes this project and a plausible plan about an imaginary one.",
      "bullets": [
        "A director that guesses durations, invents voice ids, or assumes upload properties produces plans that fail validation at best, burn GPU at worst.",
        "Compact state INLINE (duration, enrolled voices, gesture enum, current plan); read-tools for what must be probed: <code>probe_media</code>, <code>get_intake_report</code>, <code>estimate_stage</code>, <code>check_cache</code>.",
        "A well-grounded director is visibly different in logs: probes the upload BEFORE choosing the path, lists voices before assigning (never inventing an id), checks cache before estimating.",
        "The preview then says \"~40 seconds — voice and coefficients reused\" instead of scaring the creator with the full-render number. Grounding is <b>THIS project vs a plausible imaginary one</b>."
      ]
    },
    {
      "heading": "The lifecycle: draft, validate, repair, preview, approve, execute",
      "narration": "The draft plan hits the unified validator — every check this course has written, run as one gate. Stage-list sanity, Part One's allowlist and ordering. Gesture bounds: times within duration, amplitudes in range, library membership. Voice authorization: the id exists and the consent registry authorizes this creator — the one check that is a hard stop, never repaired. Parameter ranges against hardware limits. And cross-field coherence: gestures present means a motion stage present; live mode means no ambient layer. Failures return as the named-value error strings the repair loop was built for; one or two round trips fix the fixable, and what remains goes to the creator as honest questions, not silent guesses. Then the step naive designs skip: the preview. Before anything runs, the creator sees the plan in plain language — five stages, her cloned voice, wave at two seconds, lean-in at eight, gentle hair motion, roughly forty seconds because audio and coefficients are reused — with the cost estimate from the grounding tools. Approval is a click, and the approved plan goes to last lesson's executor verbatim. The director cannot slip work past the preview, because the executor runs the previewed document, not a re-generation of it. Only on the merchant's signature does Iceburg's order go to the dock, and it goes verbatim — the foreman executes the signed document, not Iceburg's recollection of it. The producer signs the page; the set shoots the page. This is the same receipts discipline as the edit session — echo what will be done, derived from the artifact — applied before the fact instead of after. For edits, act-then-echo fits because edits are cheap and revertible; for renders that cost minutes of GPU, preview-then-approve fits because the expensive thing has not happened yet. One principle, two tempos.",
      "bullets": [
        "The draft hits the <b>unified validator</b> — every check as one gate: stage sanity, gesture bounds, voice authorization (a hard stop, never repaired), parameter ranges, cross-field coherence.",
        "Failures return as named-value error strings; the repair loop fixes the fixable in 1-2 rounds; the rest go to the creator as honest questions.",
        "Then the step naive designs skip: <b>the preview</b>. Plain language + cost estimate. Approval is a click; the approved plan goes to the executor <b>VERBATIM</b>.",
        "One receipts principle, two tempos: edits are cheap → act-then-echo; renders cost minutes of GPU → <b>preview-then-approve</b>. The producer signs the page; the set shoots the page."
      ]
    },
    {
      "heading": "Plan boldly, pay marginally — and the lines never crossed",
      "narration": "Here is the elegance the last two lessons were building toward: the director always emits the complete plan — every stage, every parameter — and never reasons about incremental execution, because it does not have to. The executor computes each stage's content key; unchanged inputs hit cache; the render costs only the delta. Creator tweaks one gesture, the director re-plans the whole document — cheap, one LLM call — and execution re-runs frames and mux only. Iceburg writes complete orders every time, even when only the rigging changed, because the ledger's stamps decide what actually needs doing, and the ledger does not make his kind of mistakes. This division keeps the probabilistic component out of the incremental-correctness business entirely: the LLM never gets to be clever about what to skip — the hashes decide, deterministically. A director that tried to plan increments would reintroduce every stale-cache bug class as hallucinations; a director that plans wholes cannot cause them. And the standing boundaries, now as a complete list. The director proposes: plans, estimates, revisions with error context. It never executes: no shell, no file paths — artifact names only, the executor owns the filesystem — no direct model invocation. It never spends silently: plans above a cost threshold, and all replans, route through the preview. It never decides consent: voice authorization is the registry's verdict; a plan referencing an unauthorized voice is rejected, not repaired. A wealthy client once offered Iceburg triple for a warship rig his license did not cover, and the refusal took four words: the registry says no — the order never existed, there was nothing to revise. Replanning after failures is bounded like every loop: error context in, alternative plan out, no plan-hash repeats because retrying the identical plan is the definition of hoping, and a hard cap at two alternatives before the failure goes to the human with the full trail. Two alternatives is Iceburg's own limit — a third failure is not a planning problem, it is a conversation with the customer. The director is the most capable component in the system and the least trusted. That combination is not a paradox; it is the design. Iceburg never picks up a hammer, because the day he hammers, his mistakes stop being paper — a wrong line in an order costs a revision, a wrong cut costs a hull.",
      "bullets": [
        "The director <b>always emits the complete plan</b> and never reasons about incremental execution. The executor's content keys make unchanged stages free; the render costs only the delta.",
        "A director that TRIED to plan increments would reintroduce every stale-cache bug class as hallucinations. Hashes decide what to skip — deterministically.",
        "The boundaries, complete: it <b>proposes</b> (plans, estimates, revisions). Never <b>executes</b> (no shell, no paths, no direct model calls). Never <b>spends silently</b>. Never <b>decides consent</b>.",
        "Replanning is bounded: error context in, alternative out, <b>no plan-hash repeats</b>, hard cap at 2, then the human. The most capable component and the least trusted — that is the design."
      ]
    },
    {
      "heading": "What carries forward",
      "narration": "Step back and see what carries forward. The split works because it assigns each side its native strength — unbounded language understanding to the probabilistic component, correctness and money to deterministic code — and it prices mistakes asymmetrically: a wrong plan costs a validation round or a revision on paper, while nothing the director can output costs a hull. Is this over-engineered for a single-user local app? Concede the ceremony's weight, not its existence: auto-approve plans below a cost threshold, collapse the preview into a dismissible toast for cheap re-renders, keep the repair loop silent when it succeeds. But hold, immovably, on four things. The schema-constrained plan itself is not bureaucracy, it is the interface — the editing UI, the cache keys, and the executor all speak it. The policy gate: consent checking is exactly as necessary at one user, and cost and content gates are two if-statements. Verbatim execution of validated plans, because run-what-the-model-says without validation means the first hallucinated parameter burns GPU or produces the silent stale-output class. And bounded replanning, the one loop where an unattended system can spend an afternoon of GPU retrying a doomed plan. How do you know the director is any good, and keep it good across upgrades? Three measurable layers, harness built before you need it. Validity: fraction of plans passing the validator on first emission. Fidelity: a golden set of real request paragraphs with expected-plan properties as assertions — mentions waving twice means two wave gestures, asks for her cloned voice means the right id and mode — the tool-sequence golden-set pattern at plan scale. Judgment: the fuzzy layer, sampled and human-reviewed, and regression-diffed — same golden request, old model versus new, diff the plans and review only the diffs, cheap precisely because plans are canonical documents. And where does taste live, when a plan validates perfectly but schedules a wave over the thesis sentence? Three places: grounding enrichment, because most bad taste is missing information — feed the director the script's emphasis structure and its heuristics get real; policy-as-validation, promoting stable judgments into advisory warnings the creator can accept or fix; and the feedback loop, because the edit session is a taste-labeling machine — when creators consistently move the director's waves later, those provenance-tagged corrections are training signal you already log. What you never do is let the system auto-apply learned preferences silently, because authorship is the product. The director proposes; the human remains the court of final appeal.",
      "bullets": [
        "The architecture assigns each side its native strength: <b>unbounded language understanding</b> to the probabilistic component, <b>correctness and money</b> to deterministic code.",
        "Not over-engineering for a solo local app: auto-approve below a cost threshold, but hold the <b>schema plan</b> (it's the interface), the <b>policy gate</b>, <b>verbatim execution</b>, and <b>bounded replanning</b>.",
        "Evaluate in 3 layers: <b>validity</b> (first-emission pass rate), <b>fidelity</b> (golden set of request→plan-property assertions), <b>judgment</b> (sampled human review, diff old vs new model).",
        "\"Taste\" lives in <b>grounding enrichment</b> (feed it emphasis structure), <b>policy-as-advisory-validation</b>, and <b>mining the edit log</b> — while the human stays the court of final appeal."
      ]
    }
  ]
};
