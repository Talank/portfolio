/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["backend-api-design"] = {
  "id": "backend-api-design",
  "title": "The Backend: Uploads, Long Jobs & Live Progress",
  "titleNe": "The Baratie's spike: one ticket, no doubles, a board all can read",
  "intro": "every mechanism has lived in one process — now draw the line between the creator's browser and the machine that does the work",
  "slides": [
    {
      "heading": "The upload contract: verify what arrives, not what the filename claims",
      "narration": "The Baratie's kitchen ran on chaos until Sanji bolted a spike by the pass and a board to the wall, and the very first thing that spike enforces is that a ticket only enters the system if it is a real order, checked at the pass — not whatever a flustered waiter scrawled on the way past. Our upload boundary is that pass. A creator drags in a file named vacation_photo.jpg, and the backend must not trust the extension, must not trust the declared content-type header, must not even trust the file size the browser reported, before treating those bytes as safe input to a vision model. The contract is concrete: read the first bytes and check the actual magic number against an allowlist — JPEG, PNG, WAV, MP3, the exact formats the multimodal and TTS lessons support — never whatever ffmpeg happens to parse, which is an attack surface wearing a feature's clothes. As the bytes stream to disk, compute the content hash incrementally, the very same hash the caching lesson keys stages on, so paying for it once at the door hands every downstream stage a verified key for free. Enforce the size ceiling while the bytes are still arriving, not after they finish, so a four-hundred-megabyte attack dies at the ceiling instead of filling the disk. And because a two-minute reference video for voice cloning is easily hundreds of megabytes, add resumability: fixed-size chunks tagged by offset, the server acking each one, the client asking what do you have on reconnect and resuming from the acked offset instead of byte zero. It is the pipeline's content-addressed caching, lifted one layer up to the transfer itself — nothing bespoke, just the resumable-transfer pattern applied to creator media.",
      "bullets": [
        "Never trust the extension, the declared content-type, or the browser's reported size. Read the first bytes, check the <b>magic number</b> against an allowlist (JPEG/PNG/WAV/MP3), and reject before any model sees it.",
        "Compute the <b>content hash</b> incrementally as bytes stream to disk — the SAME hash the caching lesson keys stages on, so every downstream stage inherits a verified key for free.",
        "Enforce the size ceiling <b>while</b> bytes arrive, not after — a 400 MB attack dies at the ceiling, it does not fill the disk first.",
        "A drop at 90% must not restart a huge upload: <b>chunked + resumable</b> (offset-acked, the tus / S3-multipart shape) — the content-addressed idea, one layer up onto the wire."
      ]
    },
    {
      "heading": "202 Accepted: the response returns before the work does",
      "narration": "A waiter at the Baratie writes the order, spikes it, and tells the customer a number and a wait — instantly, before a single fish is touched — because the ticket existing IS the promise that it will be cooked. That is 202 Accepted, exactly. A DenDen Studio render can take anywhere from seconds on a full cache hit to minutes when everything is cold, and nothing about that range fits a synchronous request-response: a POST /jobs that blocks until completion either times out the HTTP connection on the slow renders or wastes a held-open socket on the fast ones, and it gives the browser nothing to show while it waits. So POST /jobs does the oldest async-API move there is — it validates the plan through the director's unified validator, enqueues it on the job queue this course already built, and returns 202 Accepted with a job_id and a status_url, typically in under fifty milliseconds regardless of how long the render will actually run. The API layer's only job at that moment is bookkeeping: a row exists, its state is queued, and the creator is holding a number, like a customer watching the board. This turns every long operation into the same shape — create, then subscribe or poll, then fetch the result when done — which is why the director's preview-then-approve flow composes so cleanly with HTTP: approval calls POST /jobs once, and everything after that is status, never a held connection. And the corollary is the one architectural rule that keeps the whole product responsive: GET /jobs/{id} must always answer instantly no matter the job's state, because it reads one row and never touches the render itself — the status board stays readable even when the kitchen is slammed.",
      "bullets": [
        "A render runs from seconds (full cache hit) to minutes (cold). A synchronous <code>POST /jobs</code> either times out the socket or holds it open for nothing — and shows the browser nothing meanwhile.",
        "<code>POST /jobs</code> validates the plan, enqueues it, returns <b>202 Accepted</b> + <code>job_id</code> + <code>status_url</code> — under 50 ms, whatever the render costs. The row exists, state is <code>queued</code>, the creator has something to hold.",
        "Every long op becomes one shape: <b>create → subscribe → fetch result</b>. The director's approve-then-render composes cleanly because approval is one POST; everything after is status, not a held connection.",
        "The rule that keeps the API alive under load: <code>GET /jobs/{id}</code> reads a row and <b>ALWAYS answers instantly</b>, however backed up the GPU queue gets."
      ]
    },
    {
      "heading": "Delivering progress: match the transport to the traffic shape",
      "narration": "Zeff's line cooks do not get asked are you done — they move the ticket themselves, FIRE, then UP, then AWAY, and anyone in the room reads the state off the board without saying a word to a cook mid-plate. That board versus a waiter pestering the line is exactly Server-Sent Events versus polling. There are three ways a browser can learn a job moved from forty percent to sixty-five. Polling — GET /jobs/{id} every N seconds — is the simplest and needs no special infrastructure, but every poll is a full round trip, progress is only ever as fresh as the interval, and N has no good value: too short wastes requests on jobs sitting in queue, too long makes a fast render feel laggy. WebSockets give a true bidirectional channel, but a progress stream is not bidirectional — the client never needs to send anything after it subscribes — so a WebSocket buys duplex capability the app pays for in reconnect logic, ping-pong keepalives, and a stateful registry of open sockets, and then never uses. Server-Sent Events fit the actual traffic: one-directional, server to client, over plain HTTP. The browser's built-in EventSource handles reconnection automatically, the server just writes data-colon-JSON lines onto a kept-open response, and it degrades gracefully through the exact same load balancers and proxies that already handle regular HTTP, with no protocol-upgrade dance. Each event carries the job's stage name, percent, and an ETA from the cache-aware estimate, plus a monotonic event id — and that id is not decoration. It is what makes reconnection loss-free, which is the next slide's whole subject, and the reason SSE beats polling in the first place: polling has no recovery story, it just asks again and hopes nothing slipped past in between.",
      "bullets": [
        "<b>Polling</b> needs no infra but every poll is a round trip, freshness caps at the interval, and no N is right — too short wastes requests on queued jobs, too long makes a fast render feel laggy.",
        "<b>WebSockets</b> give a duplex channel the progress stream never uses — the client sends nothing after subscribing — so you pay reconnect logic, keepalives, a socket registry for capability you never spend.",
        "<b>Server-Sent Events</b> fit: one-directional, plain HTTP, browser <code>EventSource</code> auto-reconnects, server writes <code>data: {...}</code> onto a kept-open response. Degrades through the same proxies as ordinary HTTP.",
        "Each event carries stage + percent + ETA and a <b>monotonic id</b> — not decoration: the id is what makes reconnection loss-free (next slide), which polling has no story for."
      ]
    },
    {
      "heading": "Idempotency & the error envelope: retries safe, failures legible",
      "narration": "One restless night a rattled new waiter, sure the first order got lost, writes a SECOND ticket for the same table and spikes it too. Sanji catches it before it fires: he checks the table number and the exact order against the ticket already moving through FIRE, sees they match, and pulls the duplicate off the spike without a word to the kitchen — the table's food gets cooked exactly once. That is an idempotency key, in aprons. A creator's browser loses network for three seconds mid-submit, and the client, following ordinary retry logic, resends POST /jobs with the identical plan; without protection, this enqueues a second render, duplicate GPU minutes for work already started. The fix is an Idempotency-Key header the client generates once per logical submission — a UUID minted at click time, resent unchanged on every retry of THAT click. The server keeps a short-lived table of key to job_id: a repeated key with a matching plan hash returns the existing job_id with no new work, and a repeated key with a DIFFERENT plan hash is a client bug, rejected as a conflict, never silently guessed at. It is the API-layer sibling of the pipeline's content-addressed caching — there, identical stage inputs are free; here, identical submissions are free, one HTTP hop earlier. And every error the API returns uses one envelope, with a code, a message, and a retryable boolean — and that boolean is the whole point. A 5xx from a worker crash is retryable, nothing about the request was wrong; a 4xx from a validator rejection is not, retrying an invalid plan just fails again forever. Baking that into the payload, rather than leaving each client to infer retry policy from HTTP status folklore, is what keeps retry logic simple and correct on every client this API ever gets — the web app today, a CLI or mobile client tomorrow, all reading the same explicit signal.",
      "bullets": [
        "A tab drops network mid-submit; the client resends the identical <code>POST /jobs</code>. Without protection that's a <b>second render</b> — double GPU minutes for one intent.",
        "An <code>Idempotency-Key</code> (one UUID minted at click, resent unchanged): key + matching plan-hash → return the EXISTING <code>job_id</code>, no new work; same key, <b>different</b> hash → conflict, never a silent guess. The API-layer sibling of content-addressed caching.",
        "One error envelope everywhere: <code>{\"error\": {\"code\", \"message\", \"retryable\"}}</code>. The <code>retryable</code> boolean is the point — 5xx worker crash retryable, 4xx validator rejection not.",
        "Baking retry policy into the payload — not leaving clients to infer it from status codes — keeps every future client (web, CLI, mobile) reading one explicit signal."
      ]
    },
    {
      "heading": "One ticket, cooked once (what carries forward)",
      "narration": "Later a dockside storm rips two tickets clean off the spike mid-rush. Instead of guessing what was lost, Zeff has the waiters read the board's written sequence — forty-one, forty-two, forty-four spiked, forty-three missing — and only forty-three gets rewritten and re-spiked, at its correct spot in line, nothing re-fired that had already gone out, nothing skipped. That is Last-Event-ID recovery with a different kind of ink. On an SSE reconnect the client sends back the last event id it processed, the server replays every buffered event past that id from a short per-job ring buffer, then continues live — nothing missed, nothing re-delivered — and it works precisely because the ids are monotonic, which turns what did I miss into a well-defined query instead of replay-everything or start-from-now. Step back and none of this lesson was new theory: it is HTTP's oldest async patterns — 202 Accepted, idempotency keys, server-sent events — wired to the job queue and cache keys the course already built, applied to the specific shape of a media pipeline, long jobs, big files, and a creator who wants to watch it happen. The through-line is a single idea: split by time-cost. Creation, status, and result are separate concerns that each answer in constant time, because no single request-response cycle can be sized to fit both a millisecond cache hit and a two-minute cold render. And because everything in this course runs locally — one GPU worker pool, usually one machine — the queue depth IS the whole system's health signal, and the API should expose it honestly, three jobs ahead of you, rather than pretend infinite capacity. But local changes only capacity planning and a few defaults; it never discounts the contracts. A locally-run vision model fed a malformed file is just as exploitable as a cloud one, so the upload-validation and idempotency discipline stay non-negotiable. Local changes the deployment topology, never the correctness obligations.",
      "bullets": [
        "None of this is new theory — it is HTTP's <b>oldest async patterns</b> (202, idempotency keys, SSE) wired to the job queue and cache keys already built, for the specific shape: long jobs, big files, a creator who wants to watch.",
        "<b>Last-Event-ID recovery</b>: on reconnect the server replays buffered events past the client's last id, THEN goes live — nothing missed, nothing doubled. The monotonic id makes \"what did I miss?\" a well-defined query; polling has none.",
        "The through-line: <b>split by time-cost</b>. Creation, status, and result each answer in constant time, because no single request cycle fits both a cache-hit and a cold render.",
        "Local-first changes capacity planning (one GPU pool, queue depth IS system health), <b>never the contracts</b> — upload validation and idempotency are correctness obligations, not cloud features."
      ]
    }
  ]
};
