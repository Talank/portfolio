/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["talking-head-fundamentals"] = {
  "id": "talking-head-fundamentals",
  "title": "Talking Heads: Audio → Lip Sync → a Still Picture That Speaks",
  "titleNe": "What gets generated vs preserved",
  "intro": "three ways to make a picture talk, the mouth that listens to the waveform, and the layer that makes it alive",
  "slides": [
    {
      "heading": "One problem, three strategies: generated vs preserved",
      "narration": "Here's the signature trick of the whole course: one still picture plus one audio track becomes video of that face speaking. Every talking-head system answers one design question — of the output's pixels, which are copied from the source image, and which are generated? Mouth-region inpainting, the Wav2Lip lineage, preserves almost everything: the frame is the source image, and it generates only the mouth area, painted per-frame to match the audio. Identity preservation is nearly perfect because it's literally the same pixels outside the mouth, and it's fast — but nothing else moves, so on a still image you get a portrait with a moving mouth, which reads as exactly that. Coefficient-driven animation, the SadTalker lineage, inserts an intermediate: audio maps to a sequence of compact motion coefficients — head pose angles, expression parameters, blink events — and a renderer then generates the image following those coefficients. The whole head lives, and crucially the representation in the middle is data, editable data. Motion transfer, the LivePortrait lineage, drives the still image with motion extracted from a real driving performance: highest naturalness in open tooling, and it tolerates stylized and illustrated faces, so the intake lesson's drawn-character route lands here. This is Usopp's Portrait Theater in the story: the paper-strip mouth, the marionette armature with its editable scroll of dial positions, and the traveling artist who repaints the whole portrait sixty times per line. The organizing spectrum, once: pixels-preserved to motion-generated, and every product decision in this Part is a position on it.",
      "bullets": [
        "<b>Mouth inpainting</b> (Wav2Lip): preserves everything, generates only the mouth. Perfect identity, nothing else moves.",
        "<b>Coefficient-driven</b> (SadTalker): audio→editable motion params→render. Whole head lives; the middle is <i>data</i>.",
        "<b>Motion transfer</b> (LivePortrait): drive the still with a real performance. Most natural; tolerates drawings."
      ]
    },
    {
      "heading": "Audio in, articulation out: the mouth listens to the waveform",
      "narration": "How does the mouth know what shape to make? The tempting design: Whisper gives word timings, words map to phonemes, phonemes map to visemes — the roughly fifteen visually distinct mouth shapes, where many phonemes share one, since B, P, and M all read as lips-pressed — and you keyframe visemes at word times. It works, and cheap real-time avatars do exactly this. But it inherits every upstream tolerance: word timestamps carry tens of milliseconds of alignment error, the Whisper lesson's warning now cashed in; phoneme durations within a word get guessed; and coarticulation — the way real mouths blur adjacent shapes, already forming the next vowel mid-consonant — is lost entirely. The eye notices all of it: sync drift beyond forty to sixty milliseconds reads as dubbed. So quality systems skip the text layer: the sync model consumes audio features directly — mel-spectrogram windows, the same representation for its fourth appearance in this course — and learns audio-to-mouth-shape as one mapping. Wav2Lip's core is exactly this, trained with a sync discriminator: a judge network pre-trained to detect audio-mouth mismatch, punishing the generator for every frame whose mouth doesn't plausibly match the sound. Two consequences worth internalizing. First, lip quality is bounded by audio quality: a muffled TTS render begets mushy articulation, because consonants live in the sharp, high-frequency transients that muffling smears — the voice column's output is this stage's input, and the pipeline is a chain of ceilings. Second, text and word timings retain exactly one sync-adjacent job: coarse alignment for captions and editing, never articulation.",
      "bullets": [
        "Tempting: word timings → phonemes → <b>visemes</b> (~15 shapes). Cheap, but inherits tens-of-ms error + no coarticulation.",
        "Quality systems skip text: the sync model consumes <b>mel-spectrogram windows</b> directly (rep's 4th appearance).",
        "Lip quality is bounded by <b>audio quality</b> — the pipeline is a <i>chain of ceilings</i>."
      ]
    },
    {
      "heading": "The aliveness layer: a synced mouth on a dead face",
      "narration": "First-time builders ship perfect lip-sync and get feedback they don't expect: creepy. The mouth is right; the face is dead. Humans blink every two to six seconds; heads perpetually micro-move, nodding fractions of a degree with speech emphasis, drifting and re-centering; chests breathe; eyes shift in tiny saccades and sometimes look at the camera, sometimes away. A face with none of this triggers the uncanny response faster than a bad mouth does — stillness is what corpses do, and the audience's hindbrain knows it. This is the discovery every builder makes, and both stories dramatize it: Usopp's unblinking portrait that stares fixed at one point for four minutes, and the animatronic that makes Raj refuse to be left alone with it — it STARES, Howard, it hasn't blinked since Tuesday. The fix is the aliveness layer: procedural idle motion composited with the speech-driven motion. Blinks on a randomized two-to-six-second clock, never metronomic, because periodicity reads as mechanical — a fixed blink is somehow creepier than none. Micro head-pose noise, low amplitude and low frequency, with slow re-centering so the head never wanders off. Emphasis nods coupled loosely to the audio energy — the same energy signal driving the mouth, one measured signal with two consumers. And breathing motion in the shoulders where the crop includes them. Coefficient-driven systems make this almost free: idle motion is just added to the coefficient streams before rendering. Inpainting systems can't do it at all — nothing outside the mouth is theirs to move — which is often the real reason a product graduates off Wav2Lip, not lip quality.",
      "bullets": [
        "Perfect lips + a motionless face = <b>\"creepy.\"</b> Stillness is what corpses do; the hindbrain knows.",
        "Blinks (irregular 2–6s), micro head-drift, breathing, emphasis nods coupled to audio energy.",
        "Coefficient systems get it almost free (add to the streams); inpainting <i>can't do it at all</i>."
      ],
      "code": "coeffs = add_idle_motion(coeffs, energy,\n    blink_s=(2, 6),     # randomized clock — metronome reads as MECHANICAL\n    drift_deg=0.4,       # low-amplitude micro head motion, slow re-center\n    nod_gain=0.15)       # emphasis nods coupled to the SAME energy signal"
    },
    {
      "heading": "Choosing for DenDen Studio: the editing feature decides",
      "narration": "How we choose isn't about the demo reel — it's about Part Four. The offline path is coefficient-driven, and the deciding argument is not render quality; it's the editing feature. The product promises fix the animation by describing the change or click-dragging it. A described edit — less head movement, bigger smile at three seconds — must become a modification to something; a drag must mutate something. Coefficient streams are that something: interpretable time-series of pose angles, expression weights, and blink events that an LLM can edit via tool calls and a canvas can expose as draggable curves. A pixels-only pipeline — pure inpainting, or end-to-end diffusion video — produces beautiful frames and no handles, and the click-drag feature dies at the architecture meeting. That's the intermediate-representations argument from lesson one, now with a specific victim. The load-bearing line in code is persisting the coefficients before rendering: it converts every future edit from re-run the model to mutate data and re-render — seconds instead of minutes. This is Usopp's editable scroll and Howard's cue sheet: less eyebrow, he looks alarmed — edit three numbers, re-run, done; you didn't touch the robot, you edited a spreadsheet of the performance. The live path takes the cheapest acceptable option — light inpainting or viseme-driven warping at reduced resolution — because the latency budget affords no better, and a slightly stiffer live avatar is an accepted trade. Both paths anchor on the same geometry: the intake detector's landmarks, never the VLM's coordinates — the multimodal lesson's jurisdiction rule paying off exactly as promised. Next Part continues here: animating from a description, and the editing architecture this slide just committed us to.",
      "bullets": [
        "Offline → <b>coefficient-driven</b>. Persist coeffs <i>before</i> rendering: edits mutate data, then re-render only.",
        "\"Less head movement, bigger smile at 0:03\" → an LLM tool-call scaling coefficient streams.",
        "Live → cheapest acceptable. Both paths anchor on the <b>detector's</b> landmarks (jurisdiction rule pays off)."
      ],
      "big": "\"A pixels-only pipeline gives beautiful frames and NO handles.\""
    },
    {
      "heading": "The chain of ceilings, and what both paths share",
      "narration": "Let's lock in the two ideas that carry forward. First, the chain of ceilings. Reference quality caps the clone, from the voice lesson; clone quality caps the articulation, from this one; and coefficient quality will cap the edit, in Part Four. Each stage's output ceiling becomes the next stage's input ceiling, and when a component seems broken, often nothing malfunctioned — the pipeline faithfully propagated degraded input. Two operational corollaries fall out: debug sync complaints by inspecting the audio first — a glance at the spectrogram distinguishes \"sync model failed\" from \"audio arrived pre-mushed\" in thirty seconds — and never insert a lossy audio re-encode between synthesis and sync; hand the sync stage the highest-fidelity audio the pipeline possesses, and compress only at final mux, because the sync expert operates on short windows and a three-frame offset at mux destroys exactly the property the whole training process fought for. Second, notice how much the offline and live paths share by design, so you build one pipeline with two profiles rather than two codebases: the intake stage and its detector geometry, one enrollment feeding both; the voice registry and consent gate, both paths speaking in enrolled voices only; and the audio-feature front end — mel windows and an energy curve computed once, consumed by articulation and emphasis motion in both modes. What's generated versus preserved, where the handles are, and which ceiling you're standing on — those three questions organize every choice in making pictures talk. Next: turning a plain-language description into that coefficient performance, and the editing loop it unlocks.",
      "bullets": [
        "Reference caps the clone → clone caps the articulation → coefficients cap the edit. <b>Each stage's output is the next's ceiling.</b>",
        "Debug sync by inspecting the <b>audio first</b>; never lossy-re-encode between synthesis and sync.",
        "Both paths share intake geometry, the voice+consent registry, and the mel/energy front end."
      ]
    }
  ]
};
