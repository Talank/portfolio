/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["creator-frontend"] = {
  "id": "creator-frontend",
  "title": "The Creator Frontend: Upload, Preview, Edit — Without Fighting the User",
  "titleNe": "Usopp's workshop table: one sketch, every tool on the same line",
  "intro": "the backend accepts, plans, and streams — this is the last mile: the screen a creator actually looks at",
  "slides": [
    {
      "heading": "One source of truth: the plan is the state, the screen is a view of it",
      "narration": "Usopp's workshop has three stations that all need to agree on one design at once: the sketch pinned to the board, the half-built prop on the workbench, and Franky's materials ledger tracking what the build will cost. Early on Usopp made the rookie mistake of letting each station keep its own notion of the design — he would adjust a measurement on the sketch, forget to update the workbench mock-up, and by the time Franky priced materials against the stale mock-up the whole afternoon's work disagreed with itself. That drift is the single biggest source of frontend bugs in an editor this shaped: the timeline says a gesture is at three-point-two seconds, the preview renders it at three-point-oh, because each view kept its own copy and only one got updated. Robin's fix is our fix: there is exactly one master sketch, pinned centrally, and every station is a window onto it, never a copy. The frontend holds exactly one plan object in memory — the same document the director emits and the executor consumes — and every visible thing, the timeline's clip blocks, the preview canvas, the cost estimate, the gesture list, is a pure render of slices of that one object. A drag handler does not move the clip on screen; it mutates plan-dot-stages-i-dot-start, and every view subscribed to that path re-renders. It is the same discipline as the pipeline's content-addressed caching and the director's single-document design, one more layer up: one canonical state, many disposable views of it. And it makes undo trivial and safe, because every edit is a mutation of one plan object, undo is restore the previous snapshot, not reverse this specific UI action and pray every dependent view noticed — the edit-list architecture from the direct-manipulation lesson already stores edits as an ordered, toggleable log for exactly this reason.",
      "bullets": [
        "The biggest bug class in an editor this shaped: two views of one fact drifting apart — timeline says t=3.2, preview renders t=3.0, because each kept its own copy and only one updated.",
        "Hold exactly ONE plan object (the same document the director emits and the executor consumes). Every visible thing — clip blocks, preview canvas, cost estimate, gesture list — is a <b>pure render of a slice</b> of it.",
        "A drag handler does not \"move the clip on screen\"; it mutates <code>plan.stages[i].start</code> and every subscribed view re-renders. One canonical state, many disposable views.",
        "Undo becomes trivial and safe: <b>restore the previous plan snapshot</b>, not \"reverse this UI action and hope every dependent view noticed.\""
      ]
    },
    {
      "heading": "Optimistic UI: move first, reconcile after — but know which edits earn it",
      "narration": "When Usopp drags a measurement line on the sketch, the workbench mock-up updates its shape immediately, right under his hand, because he is trusted to eyeball a length correctly — that adjustment does not wait on anyone's approval. That is optimistic UI. A creator dragging a gesture's timing expects the block to move under their cursor at sixty frames a second, and waiting for a server round trip on every pixel of drag would feel broken regardless of how fast the backend is. So local, in-bounds edits — drag position, an amplitude slider, a delete — apply to the in-memory plan immediately and render instantly; the change is queued for a debounced sync, not on every pixel but on drag-release, matching the direct-manipulation lesson's one-commit-per-release rule, which runs it through the same unified validator the director's output goes through. If validation fails a constraint the UI did not enforce locally — some cross-field coherence rule — the screen reconciles: the plan snaps back to the last server-confirmed state and the creator sees why, not a silent revert. But when Usopp calls out an entirely new, untested mechanism — what if it launches two things at once — the crew does not build it on a hunch; Franky checks it against what the ship's materials can actually support before a single plank is cut, because that kind of change can fail in ways an eyeball cannot catch. So not every edit deserves optimism. A drag stays entirely local because its failure modes are boring and rare. Submitting a natural-language edit request, or approving a render plan, is not optimistic — those go through the full round trip because their outcomes are uncertain enough that showing a guess and correcting it later would be worse than a brief, honest wait. The rule: optimistic for edits whose validation the UI can mostly replicate; pessimistic for anything whose validity depends on server-side facts the UI cannot see. Get it backwards — optimistic on the natural-language edit, pessimistic on the drag — and you have built exactly the sluggish, second-guessing interface creators complain about.",
      "bullets": [
        "A drag must move under the cursor at 60fps — waiting a round trip per pixel feels broken however fast the backend is. Local, in-bounds edits apply to the in-memory plan <b>immediately</b>.",
        "The change is queued for a <b>debounced sync</b> (on drag-release, not per pixel) through the SAME unified validator. If it fails a constraint the UI couldn't check, the plan <b>snaps back</b> to the last confirmed state — and shows why.",
        "Not every edit earns optimism. A drag's failures are boring (bounds the UI already enforces). A <b>natural-language edit</b> or a render approval is pessimistic — outcome depends on server-side facts the UI can't see.",
        "The rule: <b>optimistic</b> where the UI can mostly replicate validation locally; <b>pessimistic</b> where validity depends on what only the server knows. Backwards produces the sluggish, second-guessing feel."
      ]
    },
    {
      "heading": "Loading states that tell the truth: render the SSE stage, not a spinner",
      "narration": "During a long, complicated brew-and-cure step that Franky insists on watching, Usopp does not just sit there — the workshop board shows exactly what phase it is in: curing, eleven minutes left, this batch reuses last week's resin mix so it is faster than usual, instead of a vague working sign, so nobody panics or interrupts. The backend lesson built a progress stream carrying real stage names and percentages specifically so the frontend never has to show a generic spinner during a two-minute render. A spinner communicates something is happening and nothing else; a progress state built from the same events — stage frames, percent sixty-two, eta eight seconds — can say rendering frames, sixty-two percent, about eight seconds left, and when a stage is a cache hit it can say so, voice and coefficients reused, this will be fast, which is the trust-building savings line the director lesson designed the preview around, now paying off a second time in the progress UI. This only works because the frontend subscribes to the same event stream the backend was built to deliver — it is not a separate progress-estimation heuristic living in the UI, guessing independently, which would visibly lie the first time a creator hits a genuinely cold, slow render. The other discipline is distinguishing waiting for the network from waiting for the GPU from queued behind other jobs. A creator staring at queued, two jobs ahead of you, understands their situation and can walk away; a creator staring at an undifferentiated spinner for the same two minutes assumes something is broken and reloads the page — which, if the frontend is not built on the SSE reconnect discipline from the backend lesson, can actually lose their place in the stream. The honest progress state is not politeness; it is what keeps a creator from fighting the tool during the exact moments the tool is working hardest.",
      "bullets": [
        "The backend streams real stage names and percentages so the frontend never shows a generic spinner. A spinner says \"something is happening\" and nothing else.",
        "A state built from the SAME events (<code>{stage:\"frames\", pct:62, eta_ms:8000}</code>) says \"rendering frames — 62%, ~8s left\" — and on a cache hit, \"voice + coefficients reused — this will be fast,\" the director's savings line paying off again.",
        "This only works because the frontend subscribes to the <b>same stream</b> — not a separate progress heuristic guessing independently, which will visibly lie on cache-heavy or slow renders.",
        "Distinguish <b>waiting for network vs GPU vs queued behind other jobs</b>. \"Queued — 2 jobs ahead\" lets a creator walk away; an undifferentiated spinner makes them reload and lose their place."
      ]
    },
    {
      "heading": "Locking scoped narrowly: disable the one control, never the whole screen",
      "narration": "And crucially, while that cure is running, Usopp keeps sketching the next prop entirely unbothered — nothing about one station being busy locks the other two. That is scoped locking, and it is the difference between an editor that feels responsive and one that feels frozen. The laziest way to handle an in-flight request is a full-screen loading overlay that blocks every interaction until it resolves, and it is also the most damaging to a creator's sense of control: if adjusting a gesture's amplitude triggers a debounced sync, the correct scope of disabled is that one slider's immediate feedback — maybe a subtle pending indicator on it — not the timeline, not the preview, not the unrelated voice-selection panel three tabs over. Locking should be scoped to the smallest control whose precondition is genuinely unmet: the Approve and Render button disables while a plan is invalid or a preview is loading, which is its actual precondition, but the gesture timeline underneath stays fully editable, because editing gestures has no dependency on the render button's state. This scoping discipline is what makes a complex multi-panel editor feel responsive even when parts of it are legitimately busy — a creator adjusting voice settings while a previous render's progress stream is still running in a corner widget should never notice the two are related, because architecturally they are not. And the plan document's field-level granularity, the same structure the optimistic-UI section relies on, is exactly what makes this possible: lock state can live at the same granularity as the fields it protects, instead of the coarse the-app-is-busy boolean that a full-screen spinner implies. Sheldon's old habit of treating the whole lab as busy was never a fact about the work; it was a fact about his state management, and Amy's one-spec rule is what let calibration run in the corner while the grant proposal kept being drafted, untouched and unblocked.",
      "bullets": [
        "The laziest handling of an in-flight request is a full-screen overlay blocking every interaction. It is also the most damaging to a creator's sense of control.",
        "Scope \"disabled\" to the <b>smallest control whose precondition is genuinely unmet</b>. \"Approve & Render\" disables while the plan is invalid or a preview loads; the gesture timeline underneath stays fully editable — editing gestures has no dependency on that button.",
        "A creator adjusting voice settings while a previous render's progress runs in a corner should never notice the two are related — <b>because architecturally they are not</b>.",
        "The plan's field-level granularity makes this possible: lock state lives at the <b>same granularity as the fields it protects</b>, not a coarse \"the app is busy\" boolean."
      ]
    },
    {
      "heading": "Three trust moments, one contract (what carries forward)",
      "narration": "Both workshops arrive at the same rules the hard way, and both scenes end on the same detail on purpose: work continuing unblocked elsewhere while one station is legitimately busy. Step back and this frontend exists to support three trust-building moments the whole backend was built for: the upload verdict — did my file pass, told honestly at the door; the pre-render preview — here is what it will cost and what it will reuse, before you commit; and the post-edit echo — here is exactly what changed, reflected instantly. The screen is where the entire pipeline from Parts one through six either becomes invisible or leaks through as confusion. When a server-side validator rejects an optimistically-applied edit, the correct behavior is not a silent revert — the creator just watched their edit undone by an invisible hand, which reads as the app being broken. The plan snaps back to the last confirmed value and surfaces why, using the same named-value error discipline as the director's repair loop: gesture time must stay within the clip's duration, zero to eight-point-two seconds, attached to the snapped-back control. The retryable and the specific reason travel all the way from the unified validator to the exact control the creator was touching, instead of dying at the network boundary as an unexplained bounce. And the payoff of building the plan document as the shared language across the director, the executor, and the frontend is this: when the creator is not a human clicking but another program calling the API, very little changes — a script submits the same plan JSON through the same POST /jobs, idempotency key and all, and reads the same SSE stream. Optimistic UI and locking simply do not apply — there is no screen to update, no control to disable — but the error envelope's retryable field matters more, not less, because a programmatic client's retry logic has no human eyeballing a try-again dialog. Build the UI as a thin, disposable view over the plan contract rather than baking business logic into event handlers, and a CLI or automation client is never a second implementation of anything — it is the same one-source-of-truth discipline paying off at the API boundary instead of the render-tree boundary.",
      "bullets": [
        "The whole backend was built to support three UI moments: the <b>upload verdict</b>, the <b>pre-render preview</b>, and the <b>post-edit echo</b> — the screen is where the pipeline becomes invisible or leaks as confusion.",
        "A rejected optimistic edit <b>snaps back AND surfaces the reason</b> on the affected control (\"gesture time must stay within 0–8.2s\") — the error envelope's specific reason traveling all the way to the exact control, not dying at the network boundary.",
        "When the \"creator\" is another program, the plan document is the shared language: same <code>POST /jobs</code>, same idempotency key, same SSE. Optimism and locking simply don't apply — and the <code>retryable</code> flag matters <b>more</b>, with no human to read a dialog.",
        "The through-line: build the UI as a <b>thin, disposable view over the plan contract</b>, not business logic in event handlers — that's what keeps a CLI or mobile client from being a second implementation of anything."
      ]
    }
  ]
};
