/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["llm-assisted-editing"] = {
  "id": "llm-assisted-editing",
  "title": "\"Make the Smile Bigger at 0:03\": Natural-Language Edits via Tool Calls",
  "titleNe": "Nami at the speaking tube: words in, rod-marks out",
  "intro": "the second hand on the editing surface — an interpreter that writes the same records the mouse writes",
  "slides": [
    {
      "heading": "Translation with grounding",
      "narration": "The guide-handles transformed the portrait theater, but a limit emerged: the mayor could grab an arm, yet some wishes were not grabbable — the whole farewell should feel wearier, or a fix at a rod buried deep in the rig. So for Act Four, Nami takes a new station — interpreter at the speaking tube. The mayor speaks in mayor; Nami writes in rod-marks on the same overlay scroll the guide-handles write. And her first discipline is the one this whole feature stands on: she keeps the rig's current state pinned in front of her, every clip of choreography with its timing, what was last touched, what the mayor is currently pointing at — because prouder than what is the whole question. That is grounding. Make the smile bigger at three seconds only becomes an edit if the model knows what exists: which clips sit on the timeline, whether the smile is a gesture at two point eight seconds or part of the base expression, which channels are editable, the clip duration, the gesture library, what is currently selected in the editor, and what was edited last. The inline-versus-tools heuristic from Part One splits it cleanly. The compact, always-relevant state — clips with ids and times and parameters, recent edits, the selection, the duration — inlines into the system prompt as a small JSON block, because it is rarely more than a screenful and the model needs all of it every request. Anything bulky or dynamic — searching a four-hundred-item asset library, probing a rendered frame — stays behind read-tools. One non-obvious inclusion: the edit session history, because no-bigger is a complete instruction only to a model that saw its own previous edit. And one non-obvious exclusion: the dense base never inlines, not because of size but because the model has no business reasoning about frame one thousand eight hundred forty-seven of mouth-open. Its editing vocabulary is the sparse layer's vocabulary — clips, deltas, channels, times — which is exactly the vocabulary the data model was designed to make sufficient.",
      "bullets": [
        "\"Make the smile bigger at 0:03\" is only an edit if the model knows what EXISTS: clips + ids + times, editable channels, the library, <b>what's selected</b>, what was edited last.",
        "That's <b>grounding</b>: inline the compact always-relevant state (clips, recent edits, selection, duration) as a small JSON block — the model needs ALL of it every request.",
        "Include the <b>edit session history</b>: \"no, bigger\" is meaningful only to a model that saw its own last edit.",
        "The dense base <b>NEVER inlines</b> — the model has no business reasoning about frame 1,847 of mouth_open. Its vocabulary is the sparse layer's: clips, deltas, channels, times."
      ]
    },
    {
      "heading": "The edit tools: a small registry that can only say sane things",
      "narration": "Nami writes in rod-marks on the same overlay the guide-handles write, in the same notation, so Usopp's undo peels her corrections and the mayor's hand-made ones off the same stack, interleaved. That sameness is the whole architecture, and it starts with the tool registry. The registry stays small and specific — the tool-count lesson applied. Adjust-clip changes one field of a gesture clip: amplitude, start, duration-scale, hand. Add-gesture drops a gesture from the library at a time, using the description lesson's enum verbatim. Add-delta-edit lays a free-form fix on an expression channel — the LLM's equivalent of a drag. Toggle-edit and remove-edit manage existing records. And read-tools like get-frame-snapshot return a low-res render for the multimodal model to actually look at, when the creator says her eyes look wrong here — the intake VLM, moonlighting. Every argument schema is tight: channels and gestures are enums, times are numbers the validator checks against duration, values carry their min and max in the schema itself. And every write lands where the mouse's writes land: clip-field updates and provenance-llm records in the same edit list, subject to the same clamps — one source of truth for limits, the drag lesson's argument now enforced across input devices — followed by the same preview recomposite. The convergence is the whole point. Undo interleaves mouse and LLM edits in one history; A-B toggles either kind; a regenerated base re-applies both. The model gets no other write path — no raw channel arrays, no project settings, no file system. As Franky puts it, the tube and the grips are two hands: same scroll, same stops, same peel. A wish that arrives as words should be exactly as safe as a wish that arrives as a grab. The model is a hand, not an administrator.",
      "bullets": [
        "Few, specific, schema-tight tools: <code>adjust_clip</code>, <code>add_gesture</code> (library enum), <code>add_delta_edit</code> (the LLM's drag), <code>toggle_edit</code>/<code>remove_edit</code>, plus read-tools like <code>get_frame_snapshot(t)</code>.",
        "Tight args: channels & gestures are <b>enums</b>, times validated against duration, values carry min/max in the schema itself.",
        "Every write lands where the mouse's writes land: <code>provenance:'llm'</code> records in the <b>SAME edit list, same clamps, same preview</b>.",
        "The model gets <b>no other write path</b> — no raw channel arrays, no settings, no filesystem. It is a hand, not an administrator."
      ]
    },
    {
      "heading": "Ambiguity: resolve confidently, or ask one sharp question",
      "narration": "Make it bigger. Which it? Nami's resolution runs a ladder, in order. Explicit reference first: the wave at two seconds — gesture name plus time hint resolves to the nearest matching clip. Then selection state: the creator has a clip selected in the UI, and selection is context, so inline it. Then recency: no-bigger right after an edit means the just-edited object, which is why session history inlines. And only then uniqueness: one wave on the whole timeline makes the-wave unambiguous. When the reference is clear — one wave in the whole act, or the mayor's finger resting on the arm-grip — Nami writes without a word. When the ladder fails — two waves, nothing selected, no time hint — the correct move is a single concrete clarifying question naming real candidates with real times: the wave when the ship enters, or the small one at the farewell? She never guesses, because the one afternoon she guessed, the wrong wave grew grand, the mayor stopped trusting the tube, and for a week he over-explained every request like a man dictating to an enemy. Guessing wrong costs more than asking: the creator loses trust the first time the-wave edits the wrong wave, and every subsequent request gets over-specified by a user now compensating for the tool. Magnitude language needs the same discipline. A bit, more, much bigger, way less — the model should not freestyle numbers per request, because inconsistency is unlearnable: a-bit meaning plus ten percent on Monday and plus forty on Tuesday trains the creator to distrust the dial. Nami's magnitude convention is posted on the tube: a touch is one mark, plain is two, much is four, numbers spoken are numbers written. Fix the calibration as policy — a bit is fifteen percent, unqualified is thirty, much or way is sixty, all multiplicative on the current value, all clamped, absolute values used verbatim when given. Consistent calibration turns the LLM from a negotiator into an instrument: the same phrase always moves the same amount, and the creator's hand learns it like a volume knob.",
      "bullets": [
        "The resolution ladder, in order: <b>explicit reference</b> (\"the wave at 2s\") → <b>selection state</b> → <b>recency</b> (\"no, bigger\") → <b>uniqueness</b> (one wave on the timeline).",
        "When the ladder fails — two waves, nothing selected — ask ONE concrete question naming REAL candidates: \"the one at 2.1s or 14.5s?\" Never guess.",
        "Guessing wrong costs more than asking: the first wrong-wave edit turns a collaborator into a distrustful over-specifier.",
        "Magnitude is policy, not freestyle: <b>a bit=±15%, plain=±30%, much=±60%</b>, multiplicative, clamped; spoken numbers verbatim. Same phrase always moves the same amount."
      ]
    },
    {
      "heading": "The trust loop: echo, preview, revert",
      "narration": "Every applied edit gets three receipts. The echo: a plain-language, specific confirmation — increased the wave's amplitude one point two to one point six — generated from the actual tool call's arguments, not from the model's intentions. Echo what was done, not what was meant, because the two diverge exactly when it matters: a model that misfired will also mis-describe the misfire. Nami's ritual is precisely this — she reads back what she wrote, not what the mayor said: arm-rod up two marks at the harbor line, while the stagehands run the segment so he sees it move; if he frowns, the mark peels off whole. The preview: the recomposite runs immediately, scoped by the same sparsity as the drag path, so the creator sees the change before deciding anything. The revert: one click, because the edit is one record with provenance llm, and the UI shows it in the same edit list as everything else, named and attributable — why is the head tilted here? Oh, the llm edit from less-stiff-please. And guardrails keep the loop trustworthy under pressure. Per-request delta caps: a single instruction cannot move any value more than one much-step, so runaway compounding requires repeated, visible requests. The validate-repair loop from Part One on every tool call: an out-of-range time goes back to the model, which usually self-corrects in one round. Refusal to bulk-edit ambiguously: fix-everything makes the model propose a list of specific edits and ask which to apply, rather than shotgunning the timeline. And the standing invariant: the model cannot touch the dense base, cannot delete clips it did not create this session without confirmation, cannot write outside the edit list. The rig itself holds this honesty — Nami's marks obey the same joint-stops as the brass grips, and she has no key to the master chart. None of this limits a well-behaved model; all of it bounds a confused one. The feature's promise is symmetry: language and mouse are two hands on one data model, and trust comes from both hands being equally incapable of catastrophe.",
      "bullets": [
        "<b>Echo</b>: a plain-language confirmation built from the ACTUAL tool call's arguments — \"Increased the wave's amplitude 1.2 → 1.6\" — not from the model's intentions.",
        "<b>Preview</b>: the recomposite runs immediately, scoped by sparsity — the creator SEES the change before deciding.",
        "<b>Revert</b>: one click, because the edit is one record with <code>provenance:'llm'</code>, named in the same list as everything else.",
        "Guardrails: per-request delta caps, the validate-repair loop, refuse-to-bulk-edit-ambiguously, base untouchable. Both hands equally incapable of catastrophe."
      ]
    },
    {
      "heading": "What carries forward",
      "narration": "Step back and see what carries forward. The LLM does exactly one job: translate intent into references and operations — which thing, what change, how much, extracted from language. Everything around that is deterministic machinery: grounding re-inlined every turn, a small schema-tight registry writing into the same non-destructive list as the mouse, a resolution ladder in your own code deciding act-versus-ask, calibration as fixed policy, receipts derived from applied arguments. The failure modes unique to language each get a structural absorber. Referential failure — the-wave can miss — is absorbed by the ladder plus the ask-one-concrete-question floor, machinery the mouse never needs because pointing is resolution. Magnitude vagueness is absorbed by calibration policy, turning bigger into a learnable dial. Semantic overreach — fix everything, make it feel premium — is absorbed by scope rules: the model proposes a list and asks which to apply, rather than shotgunning. And silent divergence — the model believes it did X while validation clamped it to Y — is absorbed by deriving the echo from post-validation arguments, so the creator always sees Y. Notice what is not unique: out-of-bounds values, stale state, destructive mistakes threaten both hands, which is precisely why both write through one validated channel with one set of limits. And when the complaint is visual — her eyes look dead here — the session quietly composes earlier machinery: get-frame-snapshot lets the multimodal model look, dead-eyes maps onto the aliveness layer's blink density and gaze drift, and the fix is an ordinary delta edit. That composition is what the capstone means by assembly. One honest limit stated upfront: VLM aesthetic judgment is coarse — reliable for blinks-absent, unreliable for emotionally-flat — so the feature's scope is mechanical diagnosis with human taste in the loop. And the meta-lesson for creative-tool AI: adoption is a trust metric. Revert rates, correction chains, over-specification trends belong on the dashboard next to success rates from day one, because a feature that succeeds technically and dies of distrust is the failure the logs will not show you.",
      "bullets": [
        "The LLM does <b>one job</b>: translate intent → target + operation + magnitude. Everything around it — grounding, resolution, calibration, receipts — is <b>deterministic machinery</b>.",
        "Failure modes unique to language: <b>referential</b> (ladder + ask), <b>vagueness</b> (calibration), <b>overreach</b> (\"fix everything\" → propose a list), <b>silent divergence</b> (echo from applied args).",
        "Visual complaints compose earlier machinery: <code>get_frame_snapshot</code> lets the VLM look; \"dead eyes\" maps to aliveness params. The capstone is <b>assembly</b>.",
        "Adoption is a <b>trust metric</b>: watch revert rates, correction chains, over-specification — put them on the dashboard next to success rates."
      ]
    }
  ]
};
