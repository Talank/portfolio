/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["navigating-files"] = {
  "id": "navigating-files",
  "title": "Navigating & Manipulating Files: Paths, Globs, cp/mv/rm, cat/less/head/tail",
  "titleNe": "Move, copy, read — and the one command with no undo",
  "intro": "the daily file verbs, glob patterns, and how not to lose your work",
  "slides": [
    {
      "heading": "Reading files: pick the right tool",
      "narration": "Four ways to read a file, and choosing the right one is most of the skill. Cat dumps the whole thing to your screen at once, which is perfect for a short config file and a disaster for a two gigabyte log — you will be scrolling for a very long time. Less is the pager: it shows one screen at a time, you move with the arrow keys, slash searches inside it, and q quits. When in doubt, use less. Head and tail show you the beginning and the end, and for logs the end is almost always what you want, because logs append. But the one I want you to really take away is tail dash f. The f stands for follow, and it means the command does not exit — it sits there and prints new lines as they are written to the file. This is the single most useful habit in this whole lesson. You open tail dash f on the application log in one terminal, then go and reproduce the bug in another, and you watch the error appear in real time as it happens. That is how debugging on a server actually feels, and it beats guessing every time.",
      "bullets": [
        "<code>tail -f</code> on a log while you reproduce a bug is the single most useful habit here.",
        "<code>cat</code> on a 2 GB log will flood your terminal — reach for <code>less</code>."
      ],
      "code": "cat  file.txt      # dump it all (small files only)\nless file.txt      # scrollable pager — q quits, / searches\nhead -n 20 f.log   # first 20 lines\ntail -n 50 f.log   # last 50 lines\ntail -f  f.log     # FOLLOW: stream new lines as they arrive\nwc -l    f.log     # how many lines?"
    },
    {
      "heading": "Globs: letting the shell expand your patterns",
      "narration": "Globs are the wildcard patterns that let you act on many files at once, and there is one thing about them you must internalize: the shell expands them before the command ever runs. When you type rm star dot log, the rm program never sees a star. The shell first looks in the directory, finds every matching filename, and hands rm the complete list. Rm just receives a pile of filenames and deletes them. This matters enormously for safety, and it gives you a free safety drill that I want you to adopt permanently. Before you run a destructive command with a glob, run the exact same pattern with ls first. The shell expands it identically, so what ls prints is precisely the list that rm would have received. Look at it. If it is what you expected, press up-arrow, change ls to rm, and run it. That one habit will save you from the worst day of your career. And note the last line on this slide: a bare star does not match dotfiles. So a star will not catch your hidden configuration files, which is usually a mercy but occasionally a nasty surprise when you are copying a directory and wonder why the config did not come along.",
      "bullets": [
        "<b>The shell expands these, not the command.</b> <code>rm *.log</code> — rm never sees the star.",
        "Safety drill: run <code>ls</code> with the pattern first, <i>then</i> swap in <code>rm</code>."
      ],
      "code": "*.log        every name ending .log\ndata?.csv    data1.csv, dataX.csv — ? = exactly one char\nlog[0-9].txt log0.txt … log9.txt\n{a,b}.txt    a.txt and b.txt  (brace expansion)\n*            everything EXCEPT dotfiles"
    },
    {
      "heading": "cp, mv, mkdir — and their sharp edges",
      "narration": "Copy, move, and make directory — the everyday verbs. Cp copies, and for a directory you need dash r for recursive, otherwise it refuses. Mv both renames and moves, because to Linux those are the same operation: a rename is just moving a file to a new name in the same directory. Mkdir makes directories, and dash p is the flag you want almost always, because it creates any missing parent directories and does not error if the thing already exists. That makes it idempotent, meaning safe to run repeatedly, which is exactly what you want in a script. Now the sharp edge, and it is genuinely sharp. Both cp and mv will overwrite an existing file silently. No prompt, no warning, no undo, no trash can. If you copy over an important file, it is simply gone. You can pass dash i to make them ask first, and many people alias cp and mv to always include it. That is a reasonable thing to do on your own machine, though be aware that if you get used to the prompt, you will be missing it on a server where the alias is not set — so the habit of thinking before you press enter is still the real protection.",
      "bullets": [
        "<code>cp</code> and <code>mv</code> <b>overwrite silently</b> by default. No warning, no undo.",
        "<code>mv</code> is rename and move at once — a rename is just a move within one directory.",
        "<code>mkdir -p</code> is the scripting default: idempotent, safe to re-run."
      ],
      "code": "cp  file.txt backup.txt\ncp -r  src/ dest/        # -r = recursive, for directories\ncp -i  a b               # -i = ask before overwriting\nmv old.txt new.txt       # rename AND move are the same op\nmv  *.log  archive/\nmkdir -p a/b/c           # -p = create parents, no error if exists"
    },
    {
      "heading": "rm: the command with no undo",
      "narration": "This one gets its own slide because it deserves the respect. Rm deletes, and it deletes permanently. There is no trash can, no recycle bin, no undo, no control z. The bytes are unlinked and gone. Dash r makes it recursive so it can delete directories, and dash f forces it to never ask, and the combination dash r f is simultaneously the most useful and most dangerous thing in this course. Two specific ways people destroy machines with it, so you can recognize the shape. The first is a stray space. You mean to type rm dash r f dot slash old, but a space slips in after the slash, and now you have told it to delete the root of the filesystem and also a directory called old. The second is more common in scripts: you write rm dash r f dollar-DIR slash, and if that variable is empty or misspelled, it expands to nothing, and your command becomes rm dash r f slash. That has taken down real production systems more than once. The protection is the same drill as the globs: run ls with the exact path first, look at what comes back, and only then change ls to rm. Slow is fine. Gone is forever.",
      "bullets": [
        "A space in the wrong place turns <code>rm -rf ./old</code> into <code>rm -rf / ./old</code>.",
        "Never build an <code>rm</code> path from an unset variable — <code>rm -rf $DIR/</code> with empty <code>DIR</code> means <code>rm -rf /</code>."
      ],
      "code": "rm  file.txt\nrm -r  olddir/          # recursive\nrm -rf olddir/          # -f = force, never ask\n\n# The safety drill — ALWAYS:\nls  ~/logs/*.tmp        # 1. look at what matches\nrm  ~/logs/*.tmp        # 2. only then delete",
      "big": "\"There is no trash can. There is no Ctrl-Z.\""
    },
    {
      "heading": "Putting it together on a real machine",
      "narration": "Let us put the lesson together the way you will actually use it. Someone says the app is broken. Here is a sixty-second triage using only what you now know. Change into the application's log directory. Run ls dash l a t — long format, all files, sorted by modification time — so the most recently touched file is right at the top, and that is almost always the one that matters. Tail the last hundred lines of the error log to see what just happened. Then open tail dash f on it and go reproduce the problem, watching the errors appear live. That is a genuinely effective workflow and it is entirely built from this one lesson. The last line on the slide is a preview: piping into grep to filter for just the lines you care about. That is Part Three, and it is where this goes from survivable to fast. But notice you already have the shape of the thing. Next lesson is permissions — which is the other half of why things break on servers, because roughly half of all permission denied mysteries come down to a single wrong digit in a chmod.",
      "bullets": [
        "<code>ls -lat</code> — long, all, sorted by time. The newest file is usually the interesting one.",
        "You now have enough to survive on a server. Part 3 makes you fast."
      ],
      "code": "# \"the app is broken\" — a real 60-second triage\ncd /var/log/myapp\nls -lat | head            # newest files first\ntail -n 100 error.log     # what just happened?\ntail -f error.log         # now reproduce the bug\n\ngrep -i 'error' app.log | tail -20   # Part 3 makes this powerful"
    }
  ]
};
