/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["regex-fundamentals"] = {
  "id": "regex-fundamentals",
  "title": "Regex Fundamentals: Literals, ., *, +, ?, Anchors & Character Classes",
  "titleNe": "A tiny language for describing shapes of text",
  "intro": "the handful of metacharacters that do ninety percent of the work",
  "slides": [
    {
      "heading": "Regex describes a shape, not a string",
      "narration": "A regular expression is a small language for describing the shape of text rather than exact text. That distinction is the whole idea. When you search for the literal word error you are describing exact text. When you want any line containing a date, or an IP address, or a number followed by the letters M B, you are describing a shape, and regex is how you say it. Start from this: most characters simply match themselves. The letters c, a and t in a pattern match those letters. Only a small set of metacharacters are special, and learning regex is essentially learning that set. The dot is the first, and it matches any single character. Beginners routinely misuse it — if you search for three dot one four meaning the number pi, that pattern also matches three x one four, because the dot means anything. To match an actual dot you escape it with a backslash. Then square brackets define a character class, meaning exactly one character from the set inside. A dash inside gives you a range, so a to z is any lowercase letter. And a caret at the start of the brackets inverts it, so caret zero to nine means any single character that is not a digit.",
      "bullets": [
        "Most characters match themselves. Only the <b>metacharacters</b> are special.",
        "<code>.</code> is the one beginners misuse — <code>3.14</code> as a regex also matches <code>3x14</code>.",
        "Inside <code>[ ]</code>, a leading <code>^</code> means <i>not</i>, and most metacharacters lose their power."
      ],
      "code": "cat        matches the letters c-a-t, anywhere in the line\n.          ANY single character (except newline)\nc.t        cat, cot, c7t, c t …\n\n[abc]      one character: a, b or c\n[a-z]      one lowercase letter\n[0-9]      one digit\n[^0-9]     one character that is NOT a digit\n\n\\.         a LITERAL dot (backslash escapes)"
    },
    {
      "heading": "Quantifiers: how many times",
      "narration": "Quantifiers say how many times the previous thing may repeat, and this is where regex becomes genuinely expressive. Star means zero or more. Plus means one or more. Question mark means zero or one, so it marks something optional — colou question mark r elegantly matches both the American and British spellings. Curly braces let you be exact: three in braces means exactly three times, two comma five means between two and five, and two comma with nothing after means at least two. Two warnings that will save you real debugging time. First, a quantifier applies only to the single item immediately before it, not to everything you have written so far. So a b star means an a followed by any number of b's, not any number of a b pairs. To repeat a group you need parentheses, which is the next lesson. Second, and this is the classic beginner bug: star means zero or more, so a pattern of digit-star happily matches a line with no digits at all, because zero digits satisfies it. When you mean there must be at least one, use plus. And a portability note: plain grep needs backslashes before plus and braces, which is ugly, so just use grep dash E and write them normally.",
      "bullets": [
        "A quantifier applies to <b>the thing immediately before it</b>, not the whole pattern.",
        "<code>*</code> means zero or more — so <code>[0-9]*</code> matches the empty string too. Usually you want <code>+</code>.",
        "Basic grep needs <code>\\+</code> and <code>\\{ \\}</code>; <b><code>grep -E</code></b> lets you write them plainly."
      ],
      "code": "*        zero or more   (careful: matches NOTHING happily)\n+        one  or more   (ERE — needs grep -E)\n?        zero or one    (optional)\n{3}      exactly 3\n{2,5}    between 2 and 5\n{2,}     2 or more\n\ncolou?r         color and colour\n[0-9]+          one or more digits\n[0-9]{1,3}      one to three digits — an IP octet"
    },
    {
      "heading": "Anchors: where in the line",
      "narration": "Anchors are different from everything else so far, because they do not match a character at all. They match a position. A caret means the start of the line and a dollar sign means the end, and neither consumes anything — they simply assert where you are. That is why caret followed by dollar means an empty line: start of line immediately followed by end of line, with nothing in between. Anchors are what make a pattern precise. Searching for ERROR finds it anywhere, including in the middle of a sentence in a message. Searching for ERROR dollar finds only lines that end with it. And caret plus pattern plus dollar means the line must be exactly that and nothing else, which is what you want when matching a config value rather than a mention of it. Do notice the awkward thing about the caret: outside square brackets it means start of line, and inside square brackets it means not. It is the same symbol with two completely unrelated jobs, and that catches people. The word boundary, backslash b, is the other anchor worth knowing, and it is what grep dash w uses internally. And the pipeline at the bottom is a genuinely useful one to remember: strip comment lines and blank lines, and a four hundred line config file becomes the twelve lines someone actually changed.",
      "bullets": [
        "Anchors match a <b>position</b>, not a character — they consume nothing.",
        "<code>^</code> means \"start of line\" outside <code>[ ]</code>, but \"not\" <i>inside</i> them. Same symbol, two jobs.",
        "<code>grep -v '^#' file | grep -v '^$'</code> — read a config without the comments."
      ],
      "code": "^abc     line STARTS with abc\nabc$     line ENDS with abc\n^abc$    line is EXACTLY abc\n^$       an empty line\n\\bword\\b  whole word (word boundary)\n\ngrep '^#'          comment lines\ngrep -v '^$'       drop blank lines\ngrep '^\\s*$'       blank OR whitespace-only lines\ngrep 'ERROR$'      lines ENDING in ERROR"
    },
    {
      "heading": "Shorthand classes and the escaping trap",
      "narration": "Two practical matters to finish the fundamentals. First, shorthand classes. Backslash d for a digit, backslash w for a word character, backslash s for whitespace, and their capitalized forms for the negations. These are lovely and concise, but be aware they come from Perl, and plain grep does not understand them — you need grep dash capital P, or ripgrep, or a real programming language. The portable equivalents are the POSIX classes with their double square brackets, which look verbose but genuinely work everywhere including inside basic grep and sed. Second, and this one causes more confusion than any other single thing in this lesson: always wrap your pattern in single quotes. The shell processes your command line before grep ever sees it, and inside double quotes the shell will interpret backslashes, dollar signs and exclamation marks itself. So your carefully written pattern arrives at grep already mangled, and you get baffling results while staring at a pattern that looks perfectly correct. Single quotes tell the shell to pass the text through completely untouched. Make it an unconditional habit — single quotes around every regex, every time, even when it does not seem necessary. It costs nothing and removes an entire category of mystery.",
      "bullets": [
        "<code>\\d \\w \\s</code> are Perl-style: <code>grep -P</code>, ripgrep, Python — <b>not</b> portable to plain grep.",
        "<b>Always single-quote regex patterns.</b> Double quotes let the shell mangle <code>\\</code>, <code>$</code> and <code>!</code> first.",
        "POSIX classes look verbose but work absolutely everywhere."
      ],
      "code": "\\d  digit          \\w  word char [A-Za-z0-9_]     \\s  whitespace\n\\D  NOT a digit    \\W  not word char             \\S  not whitespace\n\n# POSIX — always available, works in basic grep too:\n[[:digit:]]  [[:alpha:]]  [[:alnum:]]  [[:space:]]\n\n# ALWAYS single-quote your pattern:\ngrep '\\bERROR\\b' log      # ✓ shell passes it through untouched\ngrep \"\\bERROR\\b\" log      # ✗ the shell eats the backslashes"
    },
    {
      "heading": "Building a pattern step by step",
      "narration": "Let us finish with method rather than syntax, because how you write a regex matters more than knowing every symbol. Never try to write the finished expression in one go. Build it in stages and test each stage. Suppose you want to find IP addresses in a log. Start with just a number. Refine it to one to three digits, which is an octet. Add a literal dot and a second octet, escaping the dot so it means an actual full stop. Notice the repetition, wrap the octet-and-dot in parentheses, and say it happens three times, then add the final octet. Five small steps, each one testable, and if a step breaks you know exactly which. The fastest way to test is echoing a sample string into grep, which gives you an instant feedback loop with no file needed. And notice grep dash o in there: it prints only the matching text rather than the whole line, which quietly turns grep from a filter into an extraction tool. Combined with the counting idiom, that one line pulls every IP out of a log and ranks them by frequency. Next lesson we add groups, alternation, and the greedy versus lazy distinction, which is where regex starts feeling genuinely powerful.",
      "bullets": [
        "Build in stages and test each one — never write the final regex first.",
        "<code>grep -o</code> prints <i>only the match</i>, which turns regex into an extraction tool.",
        "<code>echo 'sample' | grep -E '…'</code> is the fastest regex test loop there is."
      ],
      "code": "# Goal: find IPv4 addresses in a log\n[0-9]+                      # a number\n[0-9]{1,3}                  # 1-3 digits — an octet\n[0-9]{1,3}\\.[0-9]{1,3}      # two octets, literal dot between\n([0-9]{1,3}\\.){3}[0-9]{1,3} # three, then the last\n\ngrep -Eo '([0-9]{1,3}\\.){3}[0-9]{1,3}' access.log | sort | uniq -c\n\n# Test as you go:\necho '10.0.0.1 GET /' | grep -E '([0-9]{1,3}\\.){3}'"
    }
  ]
};
