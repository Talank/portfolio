/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["terminal-first-commands"] = {
  "id": "terminal-first-commands",
  "title": "The Terminal: Shell, Prompt, and Your First Commands",
  "titleNe": "Where you'll live for the rest of the course",
  "intro": "terminal vs shell, reading the prompt, and the commands you'll type daily",
  "slides": [
    {
      "heading": "Terminal, shell, and prompt are three different things",
      "narration": "Three words get used interchangeably and they are genuinely three different things, so let us separate them once, properly. The terminal is the window. It is a dumb display, really — it draws text on your screen and captures your keystrokes. That is its whole job. The shell is the program running inside that window. It reads what you type, works out what you meant, runs it, and shows you the result. Bash and zsh are shells. And the prompt is the little bit of text the shell prints to tell you it is finished with the last thing and ready for the next. Why bother distinguishing them? Because when you SSH into a server in Part Five, the terminal stays on your laptop while the shell is running two thousand kilometres away. If those two are one blurry concept in your head, that moment is confusing. If you have kept them separate, it is obvious: your window, their shell. Think of the terminal as the telephone handset and the shell as the person on the other end actually doing what you ask.",
      "bullets": [
        "<b>Terminal</b> — the window. Draws text, captures your keys.",
        "<b>Shell</b> — the program inside it (bash, zsh) that reads and runs commands.",
        "<b>Prompt</b> — the shell saying \"I'm ready.\""
      ]
    },
    {
      "heading": "Reading the prompt",
      "narration": "Take a proper look at the prompt, because it is not decoration — it is a status display, and once you are managing more than one machine it is the thing that saves you. Read it left to right. First your username: who the system thinks you are, which decides what you are allowed to touch. Then, after the at sign, the hostname: which machine you are on. Then the current directory, where a tilde is shorthand for your home folder. And finally the symbol: a dollar sign means you are a normal user, while a hash means you are root and every mistake you make is now permanent and system-wide. Here is why I am spending a whole slide on this. Once you start SSH-ing, you will have several terminal tabs open, and some of them are your laptop and some of them are production. The hostname in that prompt is the only thing standing between a routine cleanup command and deleting the wrong machine's files. Experienced people read the prompt before every dangerous command, the way a driver checks the mirror. Build that habit now, while nothing is at stake.",
      "bullets": [
        "After you start SSH-ing, <b>hostname</b> is the most important character on screen.",
        "<code>#</code> instead of <code>$</code> means you are root — every mistake is now permanent."
      ],
      "code": "talank@ship:~/projects$\n│      │     │      │\n│      │     │      └─ $ = normal user   (# = root, be careful)\n│      │     └──────── current directory  (~ means /home/talank)\n│      └────────────── hostname — WHICH machine you're on\n└───────────────────── username — WHO you are"
    },
    {
      "heading": "The anatomy of a command",
      "narration": "Almost every command you will ever type has the same three-part shape, and once you see it you stop memorizing commands and start reading them. First the command itself — the program to run. Then options, also called flags, which change how it behaves. Then arguments: what to act on. Flags come in two styles. A single dash takes a single letter, and you can bundle several together, so dash l dash a becomes dash l a. A double dash takes a whole word — dash dash all — which is longer to type but tells anyone reading your script exactly what you meant. Use short flags when you are typing live and long flags when you are writing something another person will read. Now, one detail that will bite you within your first week: the shell splits what you type on spaces. That is how it knows where the command ends and the arguments begin. So a file genuinely named My Notes dot txt looks like two separate arguments, and the command fails in a confusing way. The fix is quoting, and we will meet it properly in the scripting lessons — but file this away now, because that is the error you will hit first.",
      "bullets": [
        "Command, then flags, then arguments — nearly universal.",
        "Single dash takes a letter; double dash takes a word.",
        "The shell splits on <b>spaces</b> — which is why filenames with spaces need quoting."
      ],
      "code": "$ ls -l -a /var/log\n  │  │     │   │\n  │  │     │   └─ argument  (what to act on)\n  │  └─────┴───── options/flags (how to behave)\n  └─────────────── the command itself\n\n$ ls -la /var/log      # short flags can be bundled\n$ ls --all --long      # long form, self-documenting"
    },
    {
      "heading": "The handful you'll actually type every day",
      "narration": "Here is the honest short list. Not every command in Linux — the ones you will genuinely type, dozens of times a day, for the rest of your career. Pwd tells you where you are. Ls with dash l a shows you what is here including the hidden dotfiles, and hidden files matter on Linux because that is where all the configuration lives. Cd moves you around, and cd with just a dash is a lovely one: it snaps you back to the directory you were in before, like the back button in a browser. Man opens the manual — press q to quit and forward slash to search inside it — and if the manual page feels overwhelming, ls dash dash help gives you the short version. History shows what you ran before. Now let me point at the two things that separate someone who is fluent in a terminal from someone who is fighting it. First, tab completion: start typing a path, press Tab, and the shell finishes it. This is not about saving keystrokes, it is about never mistyping a path. Second, Control R: press it and start typing any fragment of a command you ran before, and the shell finds it. Learn those two today and you will thank yourself for years.",
      "bullets": [
        "<b>Tab completion</b> is not a nicety — it prevents typos in paths.",
        "<b>Ctrl-R</b> searches your history. Learn it today, thank yourself forever.",
        "<code>cd -</code> toggles between two directories like a browser back button."
      ],
      "code": "pwd            # where am I?\nls -la         # what's here, including hidden files\ncd /var/log    # go somewhere\ncd -           # go BACK to where I just was\nman ls         # the manual (q to quit, / to search)\nls --help      # the quick version\nhistory        # what did I run before?\nclear          # tidy the screen (Ctrl-L is faster)"
    },
    {
      "heading": "Everything is a file",
      "narration": "Let us finish with the idea that ties the whole system together, because it will keep paying off for the rest of the course. In Linux, everything is a file. Not just documents — your keyboard is a file, your screen is a file, your hard disk is a file, a network connection is a file, and a running process is a directory full of files. This sounds like a strange philosophical claim until you see what it buys you. Because everything looks like a file, the same small set of tools works on all of it. The commands you learn for reading a text file also read your CPU information, also read a running process's memory usage, also send data to another machine. Try this one when you have a terminal open: cat slash proc slash cpuinfo. You will get a full description of your processor. But there is no such file on your disk. When you read it, the kernel manufactures that text on the spot and hands it to you, because it is pretending to be a file so that ordinary tools can talk to it. That is the deepest design idea in Unix, and you now have it. Next lesson, we walk the filesystem itself and learn why the directories are named what they are.",
      "bullets": [
        "Your keyboard, your disk, a network socket, a running process — all appear as files.",
        "So the <i>same</i> tools (<code>cat</code>, <code>grep</code>, <code>&gt;</code>) work on all of them.",
        "<code>cat /proc/cpuinfo</code> reads your CPU. Not a file on disk — the kernel answering."
      ],
      "big": "\"Everything is a file.\""
    }
  ]
};
