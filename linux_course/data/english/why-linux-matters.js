/* Auto-generated by pres_build.py from authored content. */
window.ENGLISH_DECKS = window.ENGLISH_DECKS || {};
window.ENGLISH_DECKS["why-linux-matters"] = {
  "id": "why-linux-matters",
  "title": "Why Linux: the Landscape, the Philosophy, and Why SSH Lives Here",
  "titleNe": "A kernel, a thousand distros, one set of fundamentals",
  "intro": "what Linux actually is, and why every server on Earth boots it",
  "slides": [
    {
      "heading": "\"Linux\" is a kernel, not an operating system",
      "narration": "Let's clear up something that confuses people for years, because casual speech never distinguishes it. Linux, strictly speaking, is one program: the kernel, started by Linus Torvalds in 1991. It is the layer that talks to the actual hardware — it decides which process gets the CPU next, it manages memory, it hands out access to disks and network cards. On its own a kernel is not something you use. There is no prompt, no ls command, nothing to log into. What you actually install and use is a distribution: the kernel, plus the core command-line tools, plus a package manager, plus a set of defaults and conventions. Ubuntu, Debian, Fedora, Red Hat, Arch, Alpine — all different distributions wrapped around the same kernel. And this is genuinely good news for you, because it means the question people agonize over, which distro should I learn, is close to a non-question. You are about to learn the ninety percent that is identical everywhere. Where they diverge — mostly which package manager they use — this course calls it out explicitly.",
      "bullets": [
        "<b>Linux</b> = one program, the kernel — schedules CPU, manages memory, talks to hardware.",
        "A <b>distribution</b> = kernel + GNU tools + package manager + defaults.",
        "Ubuntu, Debian, Fedora, RHEL, Arch, Alpine — <i>same kernel underneath</i>."
      ]
    },
    {
      "heading": "The Unix philosophy explains every tool you'll meet",
      "narration": "Linux inherited its design DNA from Unix, built at Bell Labs in the nineteen-seventies, and one idea from that era explains almost every tool in this course. Write small programs that each do one thing well, that read text and write text, and let people combine them. Look at how narrow these tools are. Grep only searches text. Sort only sorts lines. Word count only counts. None of them knows the others exist. But because every one of them speaks the same language — plain text in, plain text out — you can chain them together with a pipe into combinations that no single tool's author ever imagined. This is a genuinely different instinct from a big graphical application that tries to do everything inside itself. Think of it like a Nepali kitchen: you do not own one machine that makes dal bhat. You own a pressure cooker, a tawa, a khukuri, a chalni — each simple, each good at one job, and the cook combines them into any meal. That is the shell. And it is why a huge share of how do I do this questions collapse into which two or three small tools do I chain together.",
      "bullets": [
        "<code>grep</code> only searches. <code>sort</code> only sorts. <code>wc</code> only counts.",
        "None know about each other — but all speak plain text, so pipes chain them.",
        "This is why the shell rewards <i>understanding</i> over memorizing."
      ],
      "big": "\"Small tools. Text in, text out. Compose them.\""
    },
    {
      "heading": "Why servers — and all of CI/CD — run Linux",
      "narration": "Your laptop might run macOS or Windows, but the machine your code deploys to almost certainly does not. The reasons are concrete, not just tradition. Linux is free and open source, which matters enormously when you are running ten thousand servers and would otherwise pay a licence for each one. It is extremely stable running headless — no graphical interface, just quietly serving, for months at a time. Every cloud provider defaults their images to it. And here is the reason that matters most for where this course is heading: Docker containers are Linux processes, using Linux-specific kernel features called namespaces and cgroups, which get a full lesson in Part Seven. There is no such thing as a Windows container running the same way. When you run Docker on a Mac, it is quietly starting a Linux virtual machine underneath and running your container in there. So put it together. Your pipeline runs on Linux. The containers it builds are Linux. The Kubernetes nodes that schedule them are Linux boxes. When you SSH in at eleven at night to find out why a deploy failed, you are on a Linux box using exactly the tools this course teaches.",
      "bullets": [
        "Free and open source: no per-server licence at cloud scale.",
        "Rock-stable headless for months; every cloud image defaults to it.",
        "<b>Docker containers are Linux processes</b> using Linux kernel features.",
        "\"Containers\" on Mac/Windows quietly run a Linux VM underneath."
      ]
    },
    {
      "heading": "How you'll practise: your Mac now, a real box later",
      "narration": "Good news for starting today: macOS is Unix — BSD-flavoured rather than Linux, but a close cousin — so the Terminal app already on your machine runs a real shell, and most of what you learn through Part Four behaves identically. You need to install nothing to begin. There are a few small differences and I would rather you hear them from me now than be ambushed later. macOS ships the BSD versions of some tools, notably ls and sed, and their flags differ slightly from the GNU versions that Linux servers use. Wherever that bites, the lesson calls it out as a pitfall rather than leaving you to discover it at a bad moment. Then Part Five is where this stops being a simulation. You will set up a real SSH connection to an actual Linux machine — a cheap cloud VM, a Raspberry Pi, or a spare box; the lesson walks through the options — and from that point everything runs for real, on a real remote Linux machine. Which is exactly the day-to-day reality this whole course is preparing you for.",
      "bullets": [
        "macOS Terminal is a real shell — most of Parts 0–4 behave identically.",
        "Watch for BSD vs GNU flag differences (<code>ls</code>, <code>sed</code>) — flagged as they arise.",
        "Part 5 is where you SSH into an actual Linux machine and it stops being practice."
      ]
    },
    {
      "heading": "Why this course comes before Docker",
      "narration": "Let me end with why this course exists at all, sitting where it does, right before the Docker and Kubernetes one. You could skip straight to learning Docker. Plenty of people do. They learn the commands, the pipeline works, and then one day something breaks in a way the tutorial did not cover — a permission denied inside the container, a process that will not die, a mount that is empty — and there is no foundation underneath to reason from. Here is the sentence this whole course is quietly building toward: a container is not magic, it is a Linux process. It is an ordinary process that the kernel has lied to, using namespaces, so that it sees its own filesystem, its own network, its own process IDs — and that cgroups have fenced in, so it cannot eat all the memory. That is it. That is the trick. Once you know what a process is, what a filesystem is, what a permission is, and what a namespace is, Docker stops being a black box you memorize and becomes a thin, sensible convenience over things you already understand. So: next lesson, we open the terminal and type our first commands. Let's begin.",
      "bullets": [
        "Namespaces make a process see its own filesystem, network, and PIDs.",
        "cgroups cap what it can consume.",
        "Know that, and Docker stops being a black box and becomes obvious."
      ],
      "big": "\"A container is not magic. It is a Linux process.\""
    }
  ]
};
